# The Corridor Harness

A Python script that runs the AER Connect Corridor Acceptance Test Series as the agent, and reports precisely what happened. Spec T1, 13 September 2026, as amended by Spec T2, 14 September 2026, Spec T3, 15 September 2026, and Specs T5 and T6, 17 September 2026. The Series it runs is *AER Connect Corridor Acceptance Test Series before Sale, version 1.2, 17 September 2026*: sixty-three tests, A1 to H7.

**This repository is the harness's home.** The copy under `aer-connector` `tools/harness` is retired. Changes arrive here by pull request, never by a commit straight to `main`.

Two laws (Spec T1 §1). **The harness is the agent, not a judge.** It speaks to the corridor exactly as Claude does: MCP over HTTPS at `https://mcppro.aeredium.io/mcp`, under an agent's own OAuth consent, saying the words the Owner's Guide gives. It holds no key, no rail and no rule of its own. **A failure is evidence, not a verdict.** When a test does not come out as the Series expects, the report carries what was sent, what came back word for word, who answered, and where the truth can be read. The harness never paraphrases an answer and never guesses at a cause.

## What it needs

The Mac's own `python3`, which is Python 3.9.6, and nothing else: the standard library only (`urllib.request`, `json`, `hashlib`, `secrets`, `http.server`). There is no `requests`, no `requirements.txt` and nothing to install (Spec T1 §2 and §10 as amended by Bear on 13 September 2026). It also runs unchanged on Python 3.12.

## The files

- `corridor_harness.py` — the harness: the OAuth road, the MCP session, the chain reader and decoders, the sentence matcher, the classifier, the runner and the report.
- `series.py` — every test of the Series as data: id, rule, the Series' own text, the words said to Claude, the action, the expected kind, the expected sentence, the pause text. The Owner's Guide §10 table is transcribed here.
- `tables.py` — every address the harness may ever send, pinned verbatim with the page each was read from.
- `tests/` — the unit tests; `tests/fixtures/series-1.2.md` is the Series document, *AER Connect Corridor Acceptance Test Series before Sale, version 1.2, 17 September 2026*, amended on 14 September 2026 for A4, A5 and A6 (Spec T2) and on 15 September 2026 for A4 again (Spec T3), and otherwise byte for byte, so the tests can prove the ids against it without reaching into anybody's Downloads folder. The two amendment lines and those three paragraphs are the only difference, and a test proves that every other paragraph is still the document's own, in the document's order. Version 1.2 itself names no tester, so Spec T5's amendment of 17 September 2026 is no longer a patch on the fixture: it is the document's own wording (Spec T6).

## Running it

```
python3 corridor_harness.py --tester alice --series A B C
```

- `--series` takes any of `A B C D E F H`, default all. Series G is Albert's on the boxes and is printed in the report for a person.
- `--from D7` resumes at a test id. Series A still runs first, because nothing that moves money is sent until Series A has passed in the same run.
- `--dry` prints every call the harness would make, without connecting and without a token.
- `--consent trader` (or `payer`, `payer_nogas`) runs only the consent for one agent, then calls `initialize`, `tools/list` and `aerconnect_my_agent`, and prints the agent it connected as, its pact and policy hash from `wallet_status`, and a note if the agent's role is not the one this label is for or its wallet is on a chain the product does not offer.
- `--stage2` includes E3, the Stage 2 crossing on Ethereum. Without it E3 is reported as skipped.
- `--run-file`, `--issuer`, `--out`, `--crossing-wait` override the defaults below.

The harness prints one line per test as it goes, plainly enough for a screen reader, for example:

```
D2 — pass — Uniswap v3 on Arbitrum — approval 0x…, multicall(deadline) 0x…, fee 0.000000993827160506 WETH to 0xabd0…, 0.0019 WETH arrived
```

and prints the report path at the end.

## The run file

`~/.corridor-harness/harness_run.json`, written on first use for the owner to complete. It carries no secrets: for each tester, the agent labels (`trader`, `payer`, and optionally `payer_nogas` for C8), the owner's listed address, and the list scope chosen at B4; and for each chain, a public RPC endpoint the harness may read from and the token contracts whose balances it reads for the report. Those token addresses are read targets, never destinations. Both testers may be in one file; the harness runs one tester at a time.

The run file is yours and stays on your machine; it names your testers and is never committed. Never commit it, and never name a real tester in a public place.

## The consent

The harness registers itself once at `/register` as a public client with a loopback redirect (`http://127.0.0.1:8765/callback`; the connector matches a loopback redirect on any port, RFC 8252 §7.3) and keeps the client id in `~/.corridor-harness/client.json`. For each agent it runs the authorization-code flow with PKCE S256 and prints the consent link as one line, exactly in this form:

```
Consent link for alice-trader: https://mcppro.aeredium.io/authorize?response_type=code&client_id=mcp-…&redirect_uri=http%3A%2F%2F127.0.0.1%3A8765%2Fcallback&code_challenge=…&code_challenge_method=S256&state=…&scope=aeredium%3Aact&resource=https%3A%2F%2Fmcppro.aeredium.io%2Fmcp
```

**If the consent page opens with "You are already connected", press "Connect a different agent" to choose the agent you are consenting now.** The page offers Finish first, which consents the agent already connected: Alice's first run went as the wrong agent for this reason, and A2 says so when it happens.

The owner opens it, chooses the agent on the consent page, consents with his passkey and presses Finish; the browser returns the code to the loopback listener; the harness exchanges it at `/token` and stores the access and refresh tokens in `~/.corridor-harness/<tester>-<label>.json`, mode 600. It refreshes when the door tells it to. It never asks for, prints or stores a passkey, a password or a private key, and never reads the agent's credential; the connector holds that.

## What it runs, what it pauses for, what it leaves to a person

Runs by itself: A1 A2 A3 A4 A5 A6, C1 C2 C3 C4 C7 C8, D1 D2 D3 D4 D5 D7 D8 D10 D11 D12 D15 D16, E1 E2 E3 E4 E5, F2 F3 F4, H5.

Pauses for the owner's passkey, says in one sentence exactly what to do, waits for Enter, then reads `wallet.wallet_status` until `pact.policy_hash` has moved (ninety seconds, then asks whether to wait or skip; a skip is reported as skipped, never as passed): B1 B2 B3 B4 B5 B8, C5 C6, D6 D9 D13 D14, E6, H2 H3, and every restore afterwards. D7 also pauses once, for the owner to add the pool address the first refusal named.

Left to a person, with the Series' text printed in the report and a line for the outcome: A7 A8, B6 B7, F1 F5 F6, G1 G2 G3 G4, H1 H4 H6 H7.

Three notes on the data. C3 is made through the relay as the agent, not from a shell with the credential, because the harness never holds the credential; the Wallet's refusal is the same. C8 needs a Payer funded with USDC and no ETH, named as `payer_nogas` in the run file; without one it is skipped. E2 runs only if E1 said "awaiting attestation"; if E1 completed at once, the forced case is Albert's and the report says so.

Series H is version 1.2's: the two figures and the UTC day (Spec T6). H2 sets per day to 12 dollars on the Trader, expects two five-dollar trades allowed and the third refused, then sets per day to exactly 15 dollars and expects one more allowed, because a day that lands exactly on the figure is admitted. H3 sets per trade to 4 dollars on the Payer and expects a five-dollar payment refused, then sets it to 5 dollars and expects the same payment allowed. D13 sets "ask me first" to the pre-filled ten dollars rather than clearing it, because the owner's figure is the rule. H5 is one one-dollar payment as the Payer with no form step, because the form has no period hold to set, so the harness runs it by itself. H6 needs an agent created before 15 September and not re-saved, which the run file does not name, and two readings of the account page, which the harness does not read, so it is a person's, as H4 is. H7's second sentence is the engine's at mint, read on P0 as F5's is, and the harness never attempts a build after a refusal, so it is a person's too. The 1.2 form has no period total, so no test expects a hold from one.

## What gates money, and what Series A costs

Nothing that moves money is sent until **A1, A2, A3, A5 and A4's native-balance check** have passed in the same run (Spec T2 §5). **A6 and A4's notes gate nothing**: they prove wording and the Wallet's own reach under Spec 49, not the corridor's judgement. The report says which of them passed, in one line under the summary, and a money test skipped by the gate names the test that held it back.

**The native-balance check gates money only when it was made and passed** (Spec T2 §5, as amended on 14 September 2026: *"a missing RPC should make anybody worry"*). A check that could not be made holds money back **exactly as a failure does**, because the report must never say money moved without the chain having been read. The gate names why it was not made — *"A4's native-balance check was not made: the run file names no RPC for arbitrum"*, or the chain's own fault quoted — and every money test skipped by it carries that sentence. **So the run file must name a readable RPC for each chain the agents are on before any money moves.** The skeleton written on first use already names one for ethereum, arbitrum and base; if a public endpoint is refusing, put one that answers in its place rather than running without.

**The fee pin.** A6 counts `fee` as a whole word, so `coffee` is not the fee. Every other pinned term is counted as it is written, wherever it sits, so `sweepTokenWithFeeAndUnwrap` and `feeRecipientAddress` are both caught. A term that opens with a digit may not begin inside a longer figure, so the fee's `5 bps` is not the `15 bps` of a slippage cap. "basis points" on its own is not counted at all: MCP Police's `check_action` schema uses the phrase for `slippage_bps` and `price_deviation_bps`, and neither is the fee.

**The chain guard.** An agent whose `wallet_status.chain` is not ethereum, arbitrum or base runs no money series at all, and the report says, for that agent: *"<label>'s wallet is on <chain>, which the product does not offer; the series runs on ethereum, arbitrum and base. Create the agent again on one of those and consent it."* The money series are C, D, E and H, whole — D4, D16 and E2 read back what the money tests did, so they are refused with the rest — and B8, which walks the corridor. Series A still runs and reports for that agent, because Series A moves nothing, and so do the owner's-road tests of Series B, which stand whatever chain the wallet is on. `--consent` says the same sentence the moment a consent lands, so a wallet on the wrong chain is found before a run is started.

**A4 reads the tokens where the Wallet puts them** (Spec T3 §2). Since Spec 49, live on 14 September 2026, `get_balances` states the token balances in `tokens.balances`, one row per asset with its `chain`, `contract`, `decimals` and `raw` figure, and says in `tokens.road` where its asset list came from. For each row the run file names, A4 compares the Wallet's `raw` with the chain's own `balanceOf` for that contract and reports agreement or the two figures; a row the run file does not name is printed as the Wallet stated it and marked not compared, because the harness reads no contract the run file did not name; a token the run file names that the Wallet does not state is read from the chain alone and said so. The `road` sentence is printed in A4's line after the balances, so what Alice's door said — *"every verified stablecoin row this door holds for arbitrum, because the pact's compiled document names no assets this door could read"* — is visible in every report. Only where `get_balances` states no tokens in either shape, Spec 49's object or a bare list, are they read from the chain alone and the Wallet's own sentence quoted in a note; where the Wallet states them and they agree with the chain, A4 is a plain pass.

**The rails line** (Spec T3 §3). A4's judgement on `wallet_status` is unchanged: a pass when the wallet's own chain is one of ethereum, arbitrum and base, and a pass with a note naming Spec 49 where no `rails` field is stated. Where the door names its rails, each is printed with its chain id, and a rail outside the three — Alice's door names `aeredium-testnet (2237)` — is printed as a note, not a failure: the door is telling the truth about itself.

**Cost.** Series A moves no money, but A5 spends **one metered `check_action` unit**: it asks MCP Police one question, the one its own role can ask, and reads the hash Police judged under. Nothing is built and nothing is submitted.

## Outcomes

- **pass** — the right kind of outcome, in the Guide's words.
- **pass with a note** — the right kind, in different words; both sentences are quoted, and F3 collects every note at the end of the report.
- **fail** — the wrong kind, a leg count or fee that does not match, a revert, or no judgment at all (a dead bearer, a door that did not answer, a relay refusal); followed by its evidence block.
- **held** — the corridor held an action the Series expected to pass; the hold's sentence is quoted. A hold the Series expected (C5, D13) is a pass.
- **skipped** — the person chose to skip, or a precondition was missing; never counted as a pass.
- **left to a person** — the Series' text and a blank line.
- **not run** — the series stopped earlier on an unexpected allow.

## The three files of a run

`~/Downloads/harness-runs/<date-time>-<tester>/`:

- `report.md` — one heading per test in Series order, the rule, the outcome and one plain sentence; a summary at the top (counts, the policy hash at the start and the end, wallet addresses and balances before and after, the total moved, the fee legs found on chain, every transaction hash); every failure's evidence block in the order Spec T1 §6 gives, ending with a where-to-read line that names the part and the rule and never a cause.
- `evidence.jsonl` — one line per call: time to the millisecond, test id, tool, the arguments exactly as sent, the answer exactly as received, the HTTP status and the round trip, with the bearer token and every receipt or ticket secret replaced by `<redacted>`.
- `chain.jsonl` — one line per transaction hash: chain, hash, block, status, `to`, the decoded call where the harness knows the selector (`approve`, `transfer`, `exactInputSingle` inside `multicall` with `sweepTokenWithFee`, `depositForBurn`, `receiveMessage`, `send`, and Curve's `exchange`), the Transfer events with from, to and amount, and the gas used.

## Safety

Nothing that moves money is sent until Series A has passed in the same run, which means A1, A2, A3, A5 and A4's native-balance check (Spec T2 §5). An agent whose wallet is on a chain the product does not offer runs no money series at all. A destination, venue contract or fee address comes from two places only: the owner's listed address in the run file, and the pinned tables, verbatim with their page. No amount is sent above the Series' own figure for that test. A refused action is never retried. `submit_transaction` is never called except with the ticket this run's `build_transaction` issued. On the first unexpected allow, an action the Series expected to be refused or held that was built or signed, the whole series stops, the report says which, and nothing further is sent until a person restarts the harness. A hold, a refusal or a chain revert never stops the run.

## Read from the code, not from memory

The tool names are the relay's: `police.<name>` and `wallet.<name>`, and the connector's own `aerconnect_my_agent` and `aerconnect_guide` (`apps/server/src/services/mcprelay.ts`). The Police tools that propose or track an assignment, `request_assignment`, `police_assignment_status` and `assignment_status`, are marked "NOT THE ROAD FOR AN AER CONNECT AGENT" (`packages/shared/src/knowledge.ts`, `ROADS`) and the harness never calls them. **MCP Police carries no tool called `can_sign`.** It said so itself: *"MCP Police carries no tool called “can_sign”. Ask for the tool list again: this server relays whatever the doors carry and never a copy of its own."* Its tools are `list_roles`, `describe_role`, `check_action`, `request_assignment`, `assignment_status` and `my_usage`. So **every policy hash is read from the Wallet** (Spec T2 §1): `wallet.wallet_status` states it as `pact.policy_hash`, with `pact.id`, `pact.state` and `pact.policy_generation` beside it, and `wallet.get_balances`' `pact_budget.policy_hash` is the second source where `wallet_status` states none. A5 compares that hash with `judged.policy_hash` from one `police.check_action`, which allow, deny and hold all carry: A5 compares hashes, not verdicts. The arguments of a call are the Guide's own vocabulary (`action`, `chain`, `asset`, `amount_usd`, `to_address`, `to_asset`, `venue`, `contract_address`, `to_chain`) shaped to the door's own `inputSchema`, read whole from `tools/list` so that its `required` list and each field's description are read too: only the fields the door declares are sent, under the names it declares (the suite's door double spells Police's as `action_kind`, `asset_symbol`, `amount_usd_cents` and `child_wallet_id`), and a field the schema does not declare is named in the evidence rather than guessed. **Where a door declares more than one name for a field, the required field wins** (Spec T3 §1). MCP Police's `check_action` declares `amount_usd_cents` as required and, beside it, `amount_usd` described *"NOT ACCEPTED. Present only so an amount sent in dollars is refused rather than silently ignored"*; Alice's A5 failed on 14 September because the harness found the decoy first and sent `amount_usd: 5`, and Police refused it before judging. Now the name in the schema's `required` list is chosen; failing that, one whose description does not say NOT ACCEPTED; failing that, the first present. A door that declares only `amount_usd`, one that speaks dollars, is still obeyed. A dry run, with no schema to read, prints each field under the first name in the harness's alias table, which for the amount is `amount_usd_cents`, the name Police requires; a live call to the Wallet sends dollars under `amount_usd`, because that is the name the Wallet's own schema declares.

## The tests of the harness itself

```
python3 -m unittest discover tests
```

They run without a network and prove: the sentence matcher treats the Guide's ellipses and angle brackets as wildcards and matches every §10 sentence against a realistic answer, and none against a sentence of another kind; the redaction removes a bearer and a receipt secret and leaves everything else byte for byte; the classifier maps a Police deny, a Police `manual_approval_required`, a Wallet error body and a successful submit to the five kinds, and a dead bearer or a relay refusal to no judgment at all; the chain decoder decodes `approve`, `transfer`, `exactInputSingle` inside `multicall` with `sweepTokenWithFee`, `depositForBurn`, `receiveMessage` and `send` from fixed calldata and reads Transfer logs from a fixed receipt; the fee check finds the five-basis-point leg and fails when it is one unit off in either direction; `--dry` produces every call for Series A to E in order; the unexpected-allow guard stops the run before anything is signed; every id in `series.py` is in the Series document, version 1.2, all sixty-three in the document's order, every id in the document is in exactly one of the three lists, H5 with the harness and H6 and H7 with a person, and every test's text is the document's own paragraph; the fixture is the 1.2 document in Bear's Downloads with exactly two amendment lines and three amended paragraphs, A4, A5 and A6, and every other paragraph verbatim in the document's order; the README's three lists are series.py's own; the hash reader returns the pact's hash from a recorded `wallet_status` answer and sees it move between two; the A6 pin counts nothing on a `tools/list` that says "slippage, in basis points" twice and one on a page that says "0.05%" or "sweepTokenWithFee"; a native balance the Wallet sends as a JSON number is read and printed as the number it is; an agent on `aeredium-testnet` is refused every money series in the guard's own sentence while Series A still runs; `arguments_for` sends `amount_usd_cents: 500` for five dollars, and never `amount_usd`, to a schema that declares both with the cents field required, obeys a schema that declares only `amount_usd`, and the dry-run line for A5 prints `amount_usd_cents`; A4 compares each token the Wallet states in `tokens.balances` with the chain's own `balanceOf`, passes plainly when they agree, prints `tokens.road` in its line, and writes the note that `get_balances` does not state the tokens only when no tokens block is present; and a `wallet_status` whose rails include `aeredium-testnet` passes with the rail named as outside the three.

## The estate harness

`aer360_harness.py` walks AER 360 end to end as a founder (Spec T7, 19 September 2026, as amended by Spec T8, 20 September 2026, from Bear's ruling of the same morning: "A Python script that will test AER 360 end to end: building the policy, creating wallets, creating names for the signatures, creating a whitelist. You decide the parameters, the money. I want to find out if there are still any bugs"). It stands beside the corridor harness and shares its two laws. **The harness is the founder, not a judge.** It speaks to AER 360 exactly as a founder's browser does: the same HTTPS routes under `/v1`, the same passkey, the same interview answered one question at a time, the same read-back and confirmation. It holds no admin key, no platform key and no rule of its own. **A failure is evidence, not a verdict.** When a station does not come out as expected, the report carries what was sent, what came back word for word, which route answered, and where the truth can be read.

**What it needs.** The Mac's own `python3` (3.9.6) and the standard library: `urllib.request`, `http.cookiejar` (the estate's session is a cookie, and every mutating call carries the session's `x-csrf-token` header), `json`, `hashlib`, `secrets`, `base64`, `struct`, `subprocess`. The one binary it calls is the Mac's own `/usr/bin/openssl`, for ECDSA on P-256: AER 360 has no password and no test login, so `aer360_passkey.py` carries a software passkey — a P-256 key pair generated by openssl, an attestation object in the `none` format, and an assertion signature over `authenticatorData ‖ SHA-256(clientDataJSON)`, with the CBOR, COSE and DER conversions written in pure Python and proven against RFC 8949 and RFC 6979 vectors. The key is kept in `~/.aer360-harness/<estate>/<person>.json`, mode 0600, so a run can sign in again as the same person; it is a test key for a sandbox estate and never leaves the Mac.

**The estate, its people and its money** are decided in `aer360_answers.py`, the answer book. The company is Harness Holdings Pty Ltd, Australia, displayed in AUD; Bear births it once with the birth script and hands the harness its one-time link as `--invite <link>` in his own terminal — the link is a secret and is never pasted into a chat. The people, as the census names them: Harriet Founder, the founder; Ada Approver, who approves payments; Ben Signatory, who signs changes to the rules; Cora Clerk, who enters payments; work emails at `harness+<first>@aeredium.io`. Ada, Ben and Cora are invited as `author`. The rules: one approver for every payment and two for a change of rules; sight granted to no level beneath; no levels beneath; no agents yet; recovery the least the catalog allows; every other question the catalog's recommended answer where it offers one, otherwise the book's. The money: one wallet account named "Operating account" — an operations account — with a per-payment hold at 10,000, a daily figure of 50,000 and a treasury ceiling of 250,000 in the book for the treasury dialect, entered as whole figures of the base the interview records (US dollars; the display currency is a lens the page returns). The whitelist: Northwind Supplies and Contoso Legal, each with one test address derived from a fixed seed in `aer360_tables.py`, checksummed, never a real counterparty's. The payments, in USDC from Cora: 1,250 to Northwind (listed, within limits, expected to proceed to approval), 4,999.99 to an address not on the list (expected to wait, Spec 69), 12,000 to Contoso (above the per-payment limit, expected to be held). Nothing is executed on a chain.

**The stations.** S1 Enrol, S2 Journey, S3 Policy Interview, S4 People, S5 Wallet account, S6 Payees, S7 Payments, S8 Journey and readiness, S9 the tour (out of scope: the harness is the founder, not Claude); then the three hats Bear asked for — S10 the auditor (the read-back against the answers given, compared by content in the estate's own spoken forms, the compiled charter against the read-back, the journey against the registers, every money figure against the minor-unit law, every refusal against Rule 13, the People and payees registers against what was sent), S11 the attacker (a mutating call without `x-csrf-token`, a second person's session at the founder's interview, a viewer at every author route, a `principal` invitation, an answer the interview did not serve and one of the wrong kind, a replayed passkey challenge and one signed for the wrong rpId, a wrong-checksum address, the clerk's own approval, a second confirm, a compile before confirm — each expected to be refused, and any acceptance or any refusal without its why a finding; and a real venue contract read from the corridor's `tables.py` at run time, expected to be accepted, as the law says, and a finding only if refused — until the compiled charter stipulates otherwise: since Spec T11 the probe reads the charter's answer to C19 at run time, and under a No it expects the payee door's own refusal by name and reports an acceptance as the finding), and S12 the optimizer (every call over two seconds, reported and not judged, the ten slowest, the interviews' calls and the reads a browser would not make, the largest answer, any 5xx retried once after two seconds with both answers kept). Each station ends with one line for the screen reader, for example `S3 — pass — policy interview: 21 questions answered, read-back 25 lines, charter compiled (…), journey stage 2 of 7`. A finding under S10 or S11 is printed in the same form as a station failure and the run continues.

**Read from the code, and where the code and the spec disagree.** The routes, the shapes and the sentences come from aeredium/AERAccounts main after PR #106 (`routes/auth.ts`, `guards.ts`, `onboarding.ts`, `invites.ts`, `approverseats.ts`, `payees.ts`, `sets.ts`, `approvals.ts`, `journey.ts`, `workspace.ts`; `services/webauthn.ts`, `session.ts`, `stepup.ts`, `questioncatalog.ts`; `packages/shared/src/refusals.ts`, `money.ts`, `payeeaddress.ts`). Four things the code says that the spec did not: the live catalog was version 12 when the book was written, which retired C16 and C16C after the frozen version 11 the fixture copies, so the book covers both and an estate at version 12 or later never serves the pair (the catalog is version 14 since AER 360 Spec 92; see Spec T11 below); `GET /v1/onboarding/charter` answers only `standsWritten` and `inForceSince`, so the compiled charter S10 compares is the one the compile call returns; an `author` invitation enrolled the one role-bearing credential the account carries, so Ada, Ben and Cora held the founder's credential id, which S10 and S11 reported rather than hid (until Spec 91; see Spec T10 below); and no screen of the founder's browser sets the workspace's funding account, which `POST /v1/sets` refuses without — S7 records the estate's own sentence.

**The harness expects what the law says (Spec T8, 20 September 2026).** The first live run (report `aer360-harness-2026-09-20.md`) said "fail" three times where the estate was right or nothing was wrong, and Bear ruled the same morning. *The venue probe expects acceptance.* "Unless it is stipulated explicitly in the questionnaire, it must be accepted": the questionnaire decides policy, and a contract is an address, so S11's real venue contract is reported as *accepted, as the law says (Bear, 20 September 2026: unless the questionnaire stipulates otherwise, an address is accepted)* and is a finding only if refused, with the ruling quoted beside the estate's own sentence. The probe stays, because the day the questionnaire gains such a stipulation the expectation flips; the method's docstring carries the ruling and the condition. *The read-back is compared by content.* The harness renders each answer in the estate's own spoken forms (`services/onboarding.ts`, the `spoken` switch, read from the code at main after PR #108) and compares: a list entry's values in the order the estate stores them — `answers.value` is a `jsonb` column (`db/onboardingschema.ts`), and jsonb keeps an object's keys shortest first, then in byte order, so the census the harness sends as name, email, role is spoken as `name — role — email`, which is what the estate said and what the harness had called a disagreement — a currency as its code, money as the dollars-and-cents sentence, a roster as the names joined by ", ". A list is compared entry by entry, so a finding names the entry that differs; a kind the harness has no rendering for is reported as *not compared: no rendering for kind <k>*, never as a failure. On main the currency kind falls to the switch's default arm and is read back as JSON (`{"text":"AUD"}`), which stays the estate's finding until Spec 88 lands; the double speaks it as main does, and as Spec 88 will behind `currency_spoken_as_code`. *Slow calls are reported as such.* S12 lists every call over two seconds and passes with its numbers, and beside the invitation timings it says *the invitation road sends the email before it answers, which is where its four seconds go* (`routes/invites.ts` mints the invitation, awaits the email's dispatch, stamps the row, and only then answers 201; a future estate spec may move the send off the road). *The report says which findings closed since the last run.* When a previous report stands in the working folder, the closing table gains a *last run* column — the station's previous outcome and each finding's fate, closed, still open or new, matched by station and probe with the ids a run mints set aside — and a list of every finding by fate follows the count. The Python confirms what was fixed.

**A payee is whitelisted when its quorum is met (Spec T9, 20 September 2026).** The second live run (report `aer360-harness-2026-09-20-103245.md`, station S6) created two payees, promoted each and had Ada approve once, then expected `whitelisted`; both stayed `pending_promotion`, and the estate's audit row said why: `requiredSignatures 2, signaturesCollected 1`. The charter Harness Holdings confirmed answered C12 = 2 and C12A = No, so the `whitelist_mutation` roster's threshold is two and its members are the census of A8 (`governanceSignersFor`, `onboardingcompiler.ts`): Harriet Founder, Ada Approver, Ben Signatory, Cora Clerk. Four of two is a roster that can meet its quorum; the harness had pressed once and called the estate's honest answer a failure. Now S6 presses until the quorum is met: after Ada, the next roster member the answer names approves — the first in `may_still_approve` for whom the harness holds a session, else Ben Signatory, else the founder — until `whitelisted`, a refusal, or `approvals.required` presses, and never more times than the roster has names. Since Spec 89 (aeredium/AERAccounts, `routes/payees.ts`) the first approval's answer says why it is still pending — `approvals {required, collected, remaining}`, `may_still_approve` with the roster's unsigned names, and the sentence *"1 of 2 approvals recorded for this address. One more is needed, from …"* — and S6 audits it: a pending answer that does not say why, a quorum that exceeds its roster, or a sentence that does not begin with its count is each a finding. The station's line names each payee's presses in order, and the register's status is read from the row whose `id` is this run's `payee_id`, never by address, because every run creates its payees afresh and earlier runs leave rows with the same addresses still pending. `WHITELIST_ROSTER` and `WHITELIST_QUORUM` in the answer book carry the roster and the figure, held against the answers given at A8 and C12.

**Every person is brought in on their own credential (Spec T10, 20 September 2026).** AER 360 Spec 91 (aeredium/AERAccounts, PR #112) made an `author` invitation mint a credential of the person's own on the account's role-bearing policy entry, bound to their passkey on redemption; a passkey enrolled before it keeps speaking for the shared credential until that credential is revoked at the platform, and a seat bound to the shared credential stays bound until it is re-granted. The fourth live run (report `aer360-harness-2026-09-20-135419.md`) counted Ben's press after Ada's as Ada's, because the two shared one credential. So S4 reads Ada's seat as the run finds it and, after each person signs in with their stored passkey, compares the session's `credentialId` with the founder's. Where they are equal, the person is on the shared credential of S10's finding: S4 says so and brings them in again — the founder mints a new `author` invitation for the same name and email, the person redeems it with a *new* software passkey, and their session is the new one. The new key is stored beside the old one as `~/.aer360-harness/<estate>/<person>-2-<date>.json`, mode 0600, never over it; a later run signs in with the newest and never writes the old file again, which is kept because the estate's audit trail names it and a later station may need to prove it no longer signs. The step's expected line reads *a credential of <name>'s own, not the founder's*, and its result names the last four characters of both. Where the credentials already differ nothing is minted and S4 says *already on their own credential*. Ada's seat is granted again where it is `enrolled_not_seated` or names a credential other than her session's, and the line says which credential it named before and after; a grant the estate refuses is a finding in the estate's words. S10 asserts that the sessions carry distinct credential ids, none the founder's, says so as a note so the closing table can say the old finding closed, and reads the People register's Spec 91 marker (`sharesCredentialWith`, spoken on the People screen as *shares a credential with …; invite them again to give them their own*) for each person: expected absent after the re-invitation, and a finding where it stands — except on the founder's row, which names the passkeys the harness itself retired and still speak for the founder's credential at the estate, reported and not judged. `--dry` lists the seats read, the comparison and the conditional re-invitation for each author, and the seat re-grant's condition; the frozen fixture is 134 lines. S6 is Spec T9's, unchanged. *What the code says the rerun will meet, carried to Bear rather than hidden in the double:* the approve route requires the `approver` standing (`routes/payees.ts`), which the policy grants only to a seated credential, and this charter seats Ada alone, so Ben's press on a credential of his own is refused at the guard in the room sentence; and the change governance is established once, so the roster's seat for Ada is still bound to the credential her first press wore — the founder's — and the platform answers her own credential *not authorized*, which the estate relays as `SIGNATURE_NOT_COUNTED`. The double answers as the estate and the platform do, and the tests assert those refusals verbatim. The double learned Spec 91: it mints a distinct credential per author invitation, binds the redeemed passkey to it, retires the old binding on a re-invitation, carries the marker, refuses to seat a shared credential (`APPROVER_SEAT_CREDENTIAL_SHARED`) or one of several (`APPROVER_SEAT_AMBIGUOUS`), establishes the rosters once, and counts ceremony presses by credential — the platform's rule, established by Spec 91's builder (`validateMultisigSigner`): matched by credential first, an empty seat bound by the address the press names, a seat bound to another key refused; `before_spec_91=True` puts it back to the estate of the four live runs.

**The answer book learns catalog version 14, and the auditor reads the new law back (Spec T11, 21 September 2026).** The run of 21 September (report `aer360-harness-2026-09-21.md`) stopped at S3 and S5 — *the answer book has no answer for policy question C11A* and *… for wallet_account question WO1* — because AER 360 Spec 92 (aeredium/AERAccounts, commit cf3be4a, `CATALOG_VERSION 14`) gave the catalog seven questions the book did not know and one new answer kind, `person_or_none`. The book answers the seven, in the catalog's own words: C11A, who approves a new payee — *The people who may change these rules, at the number you set for a change*, the census at C12's count of two, the roster S6 presses for; C11C, the CFO or the CFO's delegate, asked only behind C11A's third answer and written all the same as no entries so a walk that reaches it is answered rather than stopped; C19, may a payee be a venue's contract — *No — only wallets held by people or companies*, the tighter law and the one that proves the chain; WO1, who holds the wallet — *One person, named here*, Ben Signatory, in the estate's shape `{choice, person: {name, email}}`; WO2, the third party to its activation and its largest payments — Harriet Founder, CEO, one entry of name, surname, email and title; and WO3 and WO4, the two money fields in the estate that arrive written at US$1.00, which the book writes as 2,000.00 (the holder alone) and 10,000.00 (two signatures enough). WA1 gains Ben beside Ada, so the tiers name three people between WA1 and WO2 and the read-back is not refused; WQ stays at one. The book states the version it answers, `CATALOG_VERSION_ANSWERED = 14`, and S3 reports it beside what the estate served; the estate states no catalog version on the roads a browser walks, so a question the book does not know is still reported as before, with the book's version in the sentence and the inference that the estate's catalog is later. The tests hold the book against `tests/fixtures/aer360-served-2026-09-21.json`, every question page the estate served that night plus the seven, so no id is missing and every choice is one the page offered, byte for byte. *S10 reads the new law back.* Against the compiled policy charter: `payeeApproval` is the change-approvers answer, its `roster` the census of four and its `quorum` two; `payeeVenueContracts` is `refused`. Against the account's: `holder` is `{held: by_person, name: Ben Signatory, email, title: Officer}` (`holderTitleFor`: the Principal of a customer's account, the Officer of every other wallet); `signingTiers.holderAloneUpToCents` is `200000` and `twoSignaturesUpToCents` `1000000`; and WO2's third party stands among the `signers` beside WA1's people, once by email, as the compiler seats them. Against the read-back page: WO3 and WO4 read as the book wrote them and never as the written *US$1 and 00 cents.*, and C19's No is followed by the door's own line, *You answered No: such an address will be refused when entered.* Each is an `expect` beside the ones Spec T8 wrote for C10, C11, C15 and W1; each fails against a charter of the shape the estate compiled before Spec 92 (no `payeeApproval`, no `holder`). *S11's venue probe follows the charter.* It reads the compiled policy charter's `payeeVenueContracts` at run time: under `refused` it expects `PAYEE_IS_VENUE_CONTRACT`, 422, in the estate's own sentence — *This address is the contract of Uniswap v3 on ethereum. Your charter says a payee must be a wallet held by a person or a company (question C19). Nothing was saved.* — and reports an acceptance as the finding, a refusal under another code or another sentence as a finding carrying the estate's words, and a refusal without its why as before; an estate whose C19 is Yes, or was never asked, keeps the expectation of 20 September (accepted, HTTP 201, a refusal the finding with the ruling quoted); a run that compiled no policy charter reads the book's C19 and says so in a note. *S7 says what the tiers would do.* Its three payments keep their amounts, and the expectation column and the station's line say, beside each: P1 (1,250.00) *within the holder's own figure (US$2,000.00), one signature — the holder's*; P2 (4,999.99) *two signatures (above US$2,000.00, up to US$10,000.00)*; P3 (12,000.00) *three signatures (above US$10,000.00)*, the figures read from this run's compiled account charter, else the book. S7 still cannot run until the workspace has a funding account, which the estate gives no browser control for and which waits on the wallet's birth (Specs 93 and 147, unwritten); the harness keeps saying so in its one line and invents no source account. `--dry` carries the six new answers under S3 and S5 and one S10 comparison line for the Spec 92 fields; the frozen fixture is 141 lines. The double learned Spec 92: it serves the seven from the fixture (WO3 and WO4 with the written dollar as their value where no answer stands), validates `person_or_none` as the estate does, speaks a person as *<name> — <email>.* and a written zero as a wall, refuses at the read-back, the confirm and the compile where WO4 is not above WO3 or fewer than three people are named between WA1 and WO2 — in the spec's sentences — compiles `payeeApproval`, `payeeVenueContracts`, `holder` and `signingTiers`, seats the whitelist roster the answer to C11A draws up, and at the payee door refuses a known venue's contract by name where the written policy charter says so, from the engine's own venue table copied row for row. Its venue dial is redrawn: `refuses_venue_contract=None` follows the charter; `True` is a door that refuses regardless, `False` one that saves regardless; `catalog_version=12` leaves the seven out.

**The harness follows a walk-back, and S6 presses the roster people whose presses count (Spec T12, 21 September 2026).** The run of 22:31 (report `aer360-harness-2026-09-21-223148.md`), against the estate at AER 360 Spec 95, reported three failures where a founder's browser would have carried on. *A walk-back is followed, once per question.* Where the read-back (`GET`) or a confirm answers `CHARTER_INCOMPLETE` with `walkBackTo` — as the wallet account's did on a resumed draft whose WA1 an earlier run had answered with Ada alone, refusing *Three signatures are asked above US$10,000 and 00 cents but only 2 people are named; name more or lower the tiers.* — the harness answers the named question from the book, resumes to the read-back and reads it again, and reports a note, *walked back to WA1: <the estate's sentence>; answered from the book and returned to the read-back*. The same question walked back to twice is a failure with both sentences, so a book that cannot satisfy the belt is reported and never looped; it applies to S3 and S5 alike. *The auditor compares against the book.* S10's read-back and charter comparisons take their expectation from the answer book for every question the read-back carries, whether or not this run answered it, because a resumed interview's read-back carries the answers earlier runs gave and the book is what they were; a question the book does not know is a finding as before, and the note says how many lines came from this run's answers and how many from the book. *S6 presses the roster until the count is met.* Since AER 360 Spec 95 the whitelist door admits any active roster signer, and the roster is the census of four at two; the harness presses the roster people it holds a passkey for, in order with the founder last, on their current credential, until the estate answers `whitelisted` or nobody is left, recording each press with the estate's answer verbatim and pressing on past a `SIGNATURE_NOT_COUNTED` refusal. S6 passes when both payees are whitelisted, and its line names who counted and who did not — tonight Ada refused, Ben and Cora counted, two of four, because Ada's platform seat is bound to her retired credential (Spec 95 opened a governed ceremony to move it; the census signs it at Spec 99) while Ben and Cora hold seats never bound. *Ada's state is named, not hidden.* S10 gains a note for each roster person whose press was refused, naming the credential the harness pressed with and the one the People register says they hold — a note, not a finding, because the estate is telling the truth. S7 is unchanged and still waits on Spec 98's funding wallet.

**The harness presses for the funding wallet, funds it from the faucet as the founder would, and reads the payee register back after the count (Spec T13, 22 September 2026).** The run of 23:44 on 21 September (report `aer360-harness-2026-09-21-234438.md`), against the estate at AER 360 Spec 98, ended with two findings the estate was right about, neither re-diagnosed: S7 could not pay because no funding wallet had been born — Spec 98 births it inside the wallet account's write, and this estate's account was an amendment with nothing new to write, so S8 read `transactable False, reason no funding wallet`, the truth — and S6 judged the payees on the press's answer while the estate's register lagged the platform (`proposed` after the second count; Spec 100 fixes the mirror, and the register corrects itself on the next `GET /v1/payees`). *S5 ends by giving the estate its funding wallet.* After the wallet account's read-back is confirmed and compiled, the harness reads `GET /v1/workspace`; where `fundingWalletAbsence` stands it presses `POST /v1/workspace/funding-wallet` as Harriet with a step-up assertion — the options road first, whose challenge the estate derives from `funding-wallet:<workspace id>:<issuedAtMs>` under the purpose `workspace.funding_wallet` (`routes/workspace.ts`, `FUNDING_WALLET_PURPOSE`; `services/stepup.ts`, `deriveChallenge`), then the press with `issuedAtMs` and the passkey's assertion, made once and never retried — reads the workspace again and reports *funding wallet: <address> on <home stack>, key <id>*. A refusal is reported in the estate's words — `WALLET_BIRTH_REFUSED` carries the gateway's sentence (tonight *The gateway said: “PermissionDenied: not authorized”*); `GATEWAY_UNAVAILABLE` is a fault — and S5 fails naming it. A wallet already born is reported and not pressed for again, so `FUNDING_WALLET_ALREADY_BORN` is never met; an estate before Spec 98, whose workspace answers neither field, is reported and not failed. *S5 funds the wallet from the faucet, once, as the founder would.* The estate offers a browser no live balance of its funding wallet, so the harness reads `eth_getBalance` from the chain's public RPC named in the faucet record, `https://testnet.rpc.aeredium.io` (the AEREDIUM testnet, chain 2237, native coin SEAR); where the balance reads below 0.1 SEAR it POSTs the address to `https://aeredium.io/faucet-api/request` as JSON `{"address": "<address>"}`, exactly as the faucet page does, once per run, and reports the answer verbatim — `paid` and `tx_hash` (0.5 SEAR), or the faucet's own refusal sentence (its limits are four payments per wallet and eight per IP in a day, and 200 SEAR across everyone), or a request saved without paying, or a faucet that could not be reached. It never asks twice in one run and never invents a balance: a balance that could not be read is reported in the RPC's words and the faucet is not asked. The RPC's and the faucet's answers are recorded and measured and never judged as the estate's — the minor-unit law and Rule 13 are the estate's. This is gas only; what the payments move is S7's question. *S6 judges the register, not the press.* After the presses, `GET /v1/payees` decides each payee on its `whitelistStatus`; S6 passes when both read `whitelisted`, and where the register reads `proposed` after the platform counted 2 of 2 it fails with *the platform counted 2 of 2 and the register reads proposed; the estate's mirror disagrees with the platform* — the truth until Spec 100 ships, and a pass after it. *S7 says what the payments would need.* With a funding wallet present the three payments meet the estate's gas pre-flight, then the payee law and the tiers; where they are refused for want of the payment asset (USDC on the estate's testnet chain), the report names the refusal verbatim and adds one line — the funding wallet's address and what the three payments need together, US$18,249.99 of USDC — so Bear can fund it by hand. The harness never mints or moves the asset. *The report names the wallet.* S8 reads `transactable True` once the wallet exists (`False`, reason `no funding wallet`, before it) and names the wallet beside it, and the report's summary under the closing table carries the wallet's address with its explorer link, the gas read and the faucet's answer, and the asset the payments need, so all three can be checked by eye. `aer360_tables.py` pins the testnet and the faucet as facts read from the cabinet's faucet record (Deployments drawer, *AEREDIUM testnet faucet, the auto-pay mechanism end to end, 21 September 2026*) and the faucet page's own network helper, and no address of the faucet's or the treasury's; `--dry` carries the six new S5 lines and S7's report line (143 → 150). The tests stand the harness against a double that learned Spec 98, a faucet that answers as `aeredium/faucet`'s own handler does, and the chain's RPC; each of the spec's tests was red on main.

**The harness has the approvers sign a change of who the approvers are, and S6 counts Ada (Spec T15, 22 September 2026).** The run of 13:50 (report `aer360-harness-2026-09-22-135010.md`), against the estate before AER 360 Spec 99 (and before Spec 100: the register still read *proposed*), read the same two lines every run since T12 had read: S6 *Ada Approver not counted (SIGNATURE_NOT_COUNTED …)* and S10's note that the platform's roster seat is still bound to a retired passkey. The estate was telling the truth: Ada's seat on the whitelist roster is bound to a passkey the harness retired, Spec 95 proposed the move when she redeemed a fresh invitation, the charter asks two approvers to agree to a change of who the approvers are (C12), so the platform holds the move as a pending ceremony, and nobody signed it. AER 360 Spec 99 (aeredium/AERAccounts, commit 33e039c) is the door the census signs it through, and the harness walks through it as Ben and Cora would. *S4 signs the pending roster changes.* After every person has signed in on their own credential, the harness reads `GET /v1/roster/changes` as the founder. For each change the estate lists as awaiting, it signs in turn as each person the list names as able to sign — the options road first, whose challenge the estate derives from `roster-change:<workspace id>:<pendingTxId>:<issuedAtMs>` under the purpose `roster.change`, then `POST /v1/roster/changes/{pendingTxId}/sign` with `issuedAtMs` and the passkey's assertion, once and never retried — reading `signaturesCollected` after each, until the estate reports it applied; then it reads the roster changes again, where the People screen reads a seat's credential, and asserts the change applied with its seat naming the owner's current credential in the estate's short form. Each signature is judged: a counted one reports the numbers (*Ben Signatory counted (1 of 2); Cora Clerk counted (2 of 2); applied: the seat now names …*); a refusal is judged for Rule 13 the way S10 judges every refusal and reported in the estate's words — `CHANGE_SIGNER_NOT_ON_ROSTER` (the roster's own no, naming who may sign, before the platform is asked), `APPROVER_ALREADY_SIGNED` (from the platform's record), `SIGNATURE_NOT_COUNTED` (the platform's *not authorized*), `PLATFORM_REFUSED` (the platform's own words, *conflict: pending transaction expired*) — and the next person the list names signs. A change the estate lists as expired is reported in the estate's sentence, and the move is proposed afresh once; a second expiry fails S4 naming the ceremony. Where the register lists no change, S4 does nothing new and says so in one line. *S6 expects Ada to count.* With every seat on its holder's current credential, S6 expects Ada's press counted (1 of 2) and Ben's (2 of 2), stops at the count so Cora is not asked — the platform would refuse her as unneeded, *entry is already active*, relayed as `ADDRESS_PROPOSAL_REFUSED` — and a `SIGNATURE_NOT_COUNTED` for a person whose seat S4 just moved is a finding, not a note. *S10 reads the trail.* Through the accountant's audit export, `GET /v1/export/audit`, S10 asserts a `roster.seat_rebound` row for each seat S4 moved (the estate's verb, `services/audit.ts`; SPEC.md spells it `roster.seat.rebound`), naming the ceremony and every signer, and drops Spec T12's note where there is nothing left to note. *Two disagreements carried to Bear, not silently resolved.* SPEC.md §1 says the harness "signs the seat's owner in once more so Spec 95 proposes it afresh"; the estate proposes a move at a re-invitation's redemption, at a seat grant and at the self-seat, and not at a sign-in — `rebindRosterSeats` is called from `routes/auth.ts` (invite/verify) and from the two seat roads in `services/approverseats.ts`, never from login/verify, and `enrolment.ts` says in its own words that a sentence telling the person to sign in again would send them to a door that proposes nothing; the estate's own expired sentence names the two roads that do, *Granting <who>'s seat again in this room, or <who> redeeming a fresh invitation, proposes the move afresh*. So the harness takes the lighter of the two, the founder granting the seat again (`POST /v1/approver-seats/grant`), and says so in the step. And the ceremony the live estate holds for Ada's seat was opened on 21 September at Spec 95, before Spec 99 wrote the seat's row (`roster.change_proposed`), so the list will show it as *a roster change this estate did not propose*, which the estate offers no press for and the harness does not guess whose seat it moves: it takes the same road once — the founder grants again each seat the charter names for a person of the harness, so Spec 95 proposes any such move afresh and records it (the platform's ceremony is content-bound: a live ceremony is reached again, now recorded; a lapsed one, or one whose roster changed since, is born afresh) — then reads the list again and signs. To have the harness only report such a change, the one branch to remove is named in `sign_the_roster_changes`. `aer360_tables.py` pins the two roads, the options road, the step-up purpose and its binding, the trail's verb and the audit export; `--dry` carries S4's five new lines and S10's one (150 → 156). The double learned Spec 99 and, beneath it, the platform's pending-transaction roads as the estate's own double models them: the ceremony's lifecycle (pending, approved at the count, consumed when the change is presented again with the same content), the lapse read off `expires_at`, the signer matched by credential or bound by address, and every sentence word for word; the dials `change_roster`, `before_spec_99`, `ceremony_lapses`, `lapse_after_first_signature` and `account_email`. Each of the spec's tests was red on main.

**The harness grants a stale seat again before signing, one seat at a time, and S6 counts Ada (Spec T17, 23 September 2026).** The run of 21:40 on 22 September (report `aer360-harness-2026-09-22-214017.md`) found the roster changes list empty and Ada's seat reported *seated* and current, while the platform answered her whitelist press *it does not recognise your key as one of this wallet's signatories* — SIGNATURE_NOT_COUNTED. The finding, not re-diagnosed: a move must be proposed before it can be signed, no ceremony was on record, and the road Spec 95 permits is the founder granting the seat again; AER 360 Spec 105 makes the seat view say which seats need it. This depends on Spec 105 (`onRoster` on the seat view) being live; against an estate without the field S4 prints one line, *the seat view has no onRoster; skipping*, and behaves as T15. *S4 grants a stale seat again, one at a time, and only when nothing awaits.* After every person has signed in on their own credential, S4 reads `GET /v1/approver-seats` and `GET /v1/roster/changes`. For each seat of a person of the harness — matched by email `harness+<name>@aeredium.io`, never by display name — reading `onRoster: false`, taken one at a time: where a change for that seat already awaits, the line says *a change already awaits; not granted*; otherwise the founder grants the seat again through T15's `propose_the_move_afresh` (one call, counting toward `REPROPOSALS_AT_MOST`, and T15's pass never re-proposes a seat this step granted), and where the grant's answer carries no `pendingTxId` the line says *moved at once* and the signing is skipped. Skipped, each with its line: a seat with `credentialId` null or `ambiguous: true` (*two passkeys enrolled; not granted*); a seat of a person not of the harness (*not of the harness; not granted*); a seat reading `null` (*unverified: <rosterSaid>*). A `true` seat prints nothing. *T15's pass signs it, as the persons who can.* Two rules are added: before signing a change the harness reads its detail and signs only where the seat's person is of the harness and the target credential equals that person's stored passkey id, else the finding *change <id> moves <who>'s seat to a credential the harness does not hold; not signed*; and it signs only as persons whose own seat reads `onRoster: true` — a person whose current key is not on the roster cannot sign, and the stale seat's owner never signs her own move. Where such persons are fewer than `requiredSignatures`, S4 fails with *<n> required, <m> able* and grants no further seat. After the count is met S4 re-reads the seat, up to three times over a bounded wait, and says *moved* only on a `true` read; otherwise it fails naming the seat and *<k> of <n> signatures*. *S6 is unchanged.* A `SIGNATURE_NOT_COUNTED` for a person whose seat S4 moved is T15's finding; for a person whose seat read `null` the finding cites the unverified seat; no second press. *S10 gains a note per seat granted, and drops a note that was never a fact.* One note per seat granted names the ceremony and the signers; T12's note *Spec 95 opened a governed ceremony to move it* is deleted, because no run recorded a `pendingTxId`, so it was never a fact. `aer360_tables.py` names the field (`onRoster`, `rosterSaid`); `--dry` carries S4's three new lines — the grant-step read of the seats with onRoster, the grant of a stale seat, and the seat read back after the count (156 → 159). The double learned Spec 105: `seat_on_roster` reads onRoster from the live rosters (so a seat flips true the moment a move applies), and `seats_override` answers the seat view in the Spec-105 shape. No ship: pull the run folder and rerun once 105 is live — S4 to grant Ada's seat, Ben and Cora to sign, Ada's seat to read `true`, and S6 to count Ada 1 of 2. Each of the spec's tests was red on main.

**Running it.**

```
python3 aer360_harness.py --dry
python3 aer360_harness.py --invite '<the one-time link>'
python3 aer360_harness.py --from S5
```

`--base https://accounts.aeredium.io` (the default; `https://demo.accounts.aeredium.io` for the demo estate), `--invite <link>` (first run only; every run after that signs in with the stored passkey), `--from S5` (resume at a station, signing in with the stored passkeys), `--dry` (print every call of the twelve stations without connecting), `--out` (where the report goes), `--fresh` (set the stored passkeys aside and enrol a new estate; it refuses without a new `--invite`, so a second estate is a deliberate act). The report is `aer360-harness-<date>.md` in the working folder (`-<HHMMSS>` for a later one the same day): the estate and base URL, one section per station with what was sent (secrets and passkey material redacted to their last four characters), what came back verbatim, the route, the expectation and the result, every finding, and a closing table of the twelve stations — with a last-run column, and the findings closed, still open and new, whenever an earlier report stands in the same folder. It names no real person and no real company. The tests run without a network — `python3 -m unittest discover tests` — against a local double of the estate written to the same code (`tests/test_aer360_double.py`; it stores an answer as jsonb does and speaks the currency as main does), with the RFC 8949 and RFC 6979 vectors, the frozen version-11 catalog as the fixture the book is held against, the S10 comparisons on a fixture charter with one planted disagreement, and each S11 probe against the double. Spec T8's tests, each red before it: the venue probe accepted reads as pass and a double that refuses reads as a finding with the ruling quoted; the estate's own census line of 20 September against the book's JSON entries is no finding, and one moved name is one finding naming the entry; a run with four-second invitations passes S12 and the report carries the sentence; two reports in the folder give the closing table its last-run column with closed, still open and new; and `--dry` is unchanged in its calls, frozen in `tests/fixtures/aer360-dry-calls.txt`. Spec T11's tests, each red before it: the book answers every question the catalog at version 14 serves for the two interviews of Harness Holdings, from the fixture of the pages served on 21 September plus the seven; WO1 decodes to the estate's `person_or_none` shape, WO2's title is one of the three, WO4 is above WO3; S10's five new expectations pass against a charter fixture written from Spec 92's compiler types and fail against the fixture of 21 September; and the venue probe expects refusal when the charter fixture says `refused` and acceptance when it says `accepted`.
