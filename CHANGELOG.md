# Changelog

Changes arrive here by pull request, never by a commit straight to `main`. Earlier changes are recorded in the README's spec references and in the git history: Spec T1 (13 September 2026) brought the corridor harness home; Specs T2 and T3 (14 and 15 September) amended A4, A5 and A6; Specs T5 and T6 (16 and 17 September) made the harness name no real tester and the fixture the Series, version 1.2.

## Spec T14 — The harness pays in cents, funds Harness Holdings from a treasury workspace whose key lives only in the enclave, credits the gas account as the sandbox may, proves the gas refusal, and S7 passes with money that moved (24 September 2026)

From SPEC.md as amended on 22 September 2026 at 22:35 (replacing the text of 02:15 entire, which kept a float wallet's key in a file — Bear, 22 September: *"we are only using threshold signatures. There is no key anywhere to be found"* — and paid gas in the stablecoin, which U3 replaced with the gas account), built after AER 360 Spec 104 (aeredium/AERAccounts, commit 8812c64, PR #122, live 23 September 2026) and platform Spec 154 (aeredium/aegiskey-access-platform, commit e7dc195; 154b, commit 451b998) went live. The finding, not re-diagnosed: the sandbox estate pays on real chains with real USDC, by design, and the book's three payments totalled US$18,249.99; under 104 the funding account is the workspace key's own address, delegated once, the payment is quoted and sponsored by the platform, and gas is bought in dollars on the workspace's gas account. Nothing in a run may need a key outside the enclave, and the harness holds what it has always held: the people's passkeys. Read from `routes/gas.ts`, `routes/workspace.ts`, `routes/sets.ts`, `services/execution.ts` (`gasAccountPreflight`, `executeSet`, `drivePayment`), `services/accountabstraction.ts` (`formatUsdCents`, `gasSetShortfallSentence`, `gasDebitWords`, `GasRoadError.clause`), `services/fundingwallet.ts`, `services/approverseats.ts` (`completeSeatOnCharterWrite`, `addressesOfCaller`), `packages/shared/src/states.ts`; and from the platform's `internal/api/router.go`, `gas_handlers.go` (`PostAdminGasAccountCredit`), `middleware.go` (`adminAuth`), `internal/gas/credit.go`, `ledger.go`.

- `aer360_answers.py`: the book pays in cents. `MONEY` scaled by one thousand — `per_payment_cents` `"1000"` (US$10.00: O2, and the payroll dialect's P3), `per_day_cents` `"5000"` (US$50.00: O1, and the trading dialect's X2), `treasury_ceiling_cents` `"25000"` (US$250.00: T1), `holder_alone_cents` `"200"` (US$2.00: WO3), `two_signatures_cents` `"1000"` (US$10.00: WO4) — and `PAYMENTS` P1 `"1.25"` (within the holder's own figure), P2 `"4.99"` (two signatures), P3 `"12.00"` (three; above the hold). Nothing else in the book changes.
- `aer360_tables.py`: the Treasury's rows — `TREASURY` (Harness Treasury Pty Ltd, `harness-treasury`, the address `harness+treasury@aeredium.io` the estate attributes to its founder's key), the admin credential's file name and keys (`ADMIN_ENV_FILE` `admin.env`, `ADMIN_ENV_URL_KEY` `AAP_ADMIN_BASE_URL`, `ADMIN_ENV_KEY_KEY` `AAP_ADMIN_KEY`, `ADMIN_KEY_PREFIX`), the admin credit road and reason (`ADMIN_CREDIT_ROUTE`, `ADMIN_CREDIT_REASON` *sandbox run %s*), `GAS_CREDIT_USD_CENTS` 1000, the estate's gas roads (`GAS_ACCOUNT_ROUTE`, `FUNDING_BALANCES_ROUTE`, `SET_EXECUTE_ROUTE`), the gate and codes (`GAS_GATE`, `GAS_SHORTFALL`, `PAYMENT_UNPRICED`, `WORKSPACE_NOT_PROVISIONED`), the trail's verb and the states (`INSTRUCTION_CONFIRMED`, `INSTRUCTION_TERMINAL_STATES`, `SET_TERMINAL_STATES`, `PLATFORM_LANDED`), the spec's sentences (`NO_GAS_CREDIT_ROAD_SENTENCE`, `TREASURY_SHORT_SENTENCE`, `FUND_TREASURY_SENTENCE`), and the helpers `format_usd_cents` (the estate's spelling, no grouping), `gas_set_shortfall_sentence`, `gas_debit_words`, `usdc_dollars` (six decimals, dust shown whole), `usdc_minor_of_cents`, `parse_env_file`, `public_rpc_url` (the AEREDIUM testnet's, else the corridor harness's own skeleton, read at run time and never copied) and `balance_of_call_data` (the ERC-20 selector).
- `aer360_harness.py`, the Treasury's birth (`for_treasury`, `bring_in_the_treasury`, `treasury_overrides`, `override_answer`; `--treasury-invite`): the Treasury's own runner shares this run's records, secrets and stamp and keeps its founder's passkey under `~/.aer360-harness/harness-treasury/`; it signs in with the stored passkey or enrols by the link; on the birth run the policy and wallet-account interviews are walked from the book with the Treasury's three answers (A1, C11, WA1) and the funding wallet is pressed for (T13's road). The Treasury's workspace is read before anything is walked (`GET /v1/workspace`), so a funding wallet that did not stand before this run marks the birth run (`born_this_run`); on that run, once the Treasury's USDC is read and found short of the shortfall, S7 prints the address and stops with *fund Harness Treasury: <address> on ethereum, then rerun* — a Treasury that stood before and is short is told *Harness Treasury holds US$<x>; the run needs US$<y>; fund <address> on <chain>*, and a Treasury born and already funded pays without a rerun. `treasury_passkey_stored` says whether an earlier run born it; the Treasury runner's calls carry the suffix *(Harness Treasury)* on their author (`who_suffix`), so Every call tells the two founders named Harriet Founder apart.
- `aer360_harness.py`, the money reads (`read_usdc_balance`, `read_gas_account`, `read_token_balance`): a workspace's USDC through `GET /v1/workspace/funding-account/balances` (the USDC row's exact `balanceMinor`; its `token` is the contract a payee's balance is read against), its gas account through `GET /v1/gas/account` (the platform's cents and *Gas account: US$…*), a payee's USDC from the chain — `eth_call balanceOf` through the chain's public RPC; a fault is the RPC's, in its words, and a proof it could not read is not a pass.
- `aer360_harness.py`, the funding payment (`treasury_pays_the_shortfall`, `pay`, `sign_the_run`, `read_until_terminal`, `read_trail`, `judge_landing`): where Holdings' USDC is below the three payments' sum, the Treasury's gas is credited and one payment of one set — a declared one-off of the shortfall to Holdings' address — is reviewed, created, submitted, approved with the Treasury founder's passkey (refused at the guard once, her seat is granted and she presses again), executed by her (`POST /v1/sets/{id}/execute`), read until its instruction is terminal, and judged on the run's status, the `userOpHash` and `txHash` of the trail's `instruction.confirmed` row (every page of `GET /v1/export/audit` by its `nextCursor`), and Holdings' balance risen by the shortfall; the gas debited rides beside. Where the Treasury is short S7 fails with *Harness Treasury holds US$<x>; the run needs US$<y>; fund <address> on <chain>* and nothing is sent.
- `aer360_harness.py`, S7a (`prove_the_gas_refusal`, `gas_shortfall_of`): with Holdings' gas account below the review's ceiling, the set of three is submitted to `POST /v1/sets/review` and the gas gate (`gas_preflight`) must refuse `GAS_SHORTFALL` in U3's sentence — *Your gas account holds US$… This set needs at most US$… of gas. Nothing was sent. Buy gas below.* — naming the `availableUsdCents` the harness read from `GET /v1/gas/account` and the gate's own `ceilingUsdCents`; the runs register must carry no new run and Holdings' USDC must stand unchanged. A gate that admits the set because the balance covers the ceiling is *S7a not proved*, reported and not failed; a refusal naming other figures, another refusal, or a payment that left is *S7a failed*, naming it.
- `aer360_harness.py`, the gas credits (`read_admin_env`, `credit_gas`, `credit_words`, `gas_credit_for`): the credential read once from `~/.aer360-harness/admin.env` and redacted from every record; one `POST <AAP_ADMIN_BASE_URL>/v1/admin/accounts/{aapAccountId}/gas-account/credits` per workspace under `Authorization: Bearer <AAP_ADMIN_KEY>` with `amount_usd_cents`, `reason` *sandbox run <run id>* and one idempotency key per credit of the run for a workspace (`aer360-harness-<run>-<workspace>-gas-<n>`, so the platform dedupes neither of two) — US$10.00, or the next ten dollars above twice the ceiling S7a read; a review that still refuses for want of gas credits once more and asks again. The answer is classified: `credited` or `deduped` with the platform's balance; 401 or 403 *refused: the platform refused the admin credential (HTTP …): <their sentence> — the answer will be the same until the credential in admin.env is replaced*; another 4xx the harness's or the deployment's mistake; a 5xx or no answer a fault. Anything but a credit fails S7 naming it; the file absent, or without its keys, fails S7 with *no gas credit road: the sandbox credits gas through the platform's admin road; file the credential in ~/.aer360-harness/admin.env*. The card road is not walked.
- `aer360_harness.py`, S7's order (`station_s7`, `walk_s7`): Harness Holdings' money is read first — its USDC and its gas account; the Treasury is brought in only where Holdings is short of the three payments, or where a Treasury passkey is stored or `--treasury-invite` is given; an estate with no funding wallet has no address to fund, so the Treasury is not brought in, S7a is *not made* and no gas is credited, each said in a note, and the three payments record the estate's own refusal (`WORKSPACE_NOT_PROVISIONED`, the wallet's sentence) at creation. Every stop S7 raises carries what S7 had said before it, once, so the line tells the whole road. `judge_landing` tells a run that never reached execution in the words it stopped with — *refused at creation — <the estate's sentence>* — and judges no proofs it could not have.
- `aer360_harness.py`, S7's judgment (`station_s7`, `TIER_ROAD_WORDS`, `SIGNERS_IN_ORDER`): each of the three payments is reviewed, created and submitted; while it waits the signers press in the spec's order (Ben, Cora, Ada, Harriet), each answer in the estate's words and never retried; it is executed by its author and judged on the four proofs, the gas debited beside; the line carries the count the estate asked and the spec's words for each (*lands with one signature, the holder's*; *waits for two and lands when Ben Signatory and Cora Clerk sign*; *waits for three and lands when the third signs*) beside Spec T11's tier words. `facts["sets"]` keeps each payment's record for S11 (the set's view, its approvals, its refusal).
- `aer360_harness.py`, S10 (`audit_the_money_moved`): one note — Harness Treasury's USDC before and after, Harness Holdings' USDC before and after, the sum of the three payments, Holdings' gas account before and after against the sum of the three gas debits — and a finding where any pair does not reconcile to the cent (*money moved: <pair> does not reconcile to the cent*), or where a landed payment's debit is not on the trail. USDC is reckoned in minor units; the gas account on `balanceUsdCents`.
- `aer360_harness.py`, `audit_money`: an integer of minor units is exact whether a string or a JSON integer — Spec 104's gas roads speak cents as JSON integers (`routes/gas.ts`) — so neither is a finding; a float, or a string that is not an integer, still is.
- `aer360_harness.py`, the report: the summary names the Treasury's funding wallet and the asset line says the Treasury pays the shortfall through the estate's own road and the harness holds no key; `payments_need_line` is retired with T13's *fund it by hand*. `--dry`: S7's block rewritten (the credential's file, the Treasury's sign-in or birth, its charter and wallet, the money before, the Treasury's credit and payment, S7a, Holdings' credit, each payment's balance reads, review, creation, submission, approvals, execution, register reads, the trail, the money after) and S10's money line; `tests/fixtures/aer360-dry-calls.txt` regenerated (159 → 193 lines).
- TWO DISAGREEMENTS CARRIED TO BEAR, NOT SILENTLY RESOLVED. (1) SPEC.md §3 credits the gas accounts after S7a, and §2 has the Treasury pay before S7; the Treasury's own payment cannot leave without gas on the Treasury's account (its quote is refused for want of gas), and the review's dry quote simulates the transfer (Spec 154: the call gas from the chain's dry run), so Holdings holding no USDC is refused for an unpriceable transfer, not for want of gas. S7 therefore credits the Treasury's gas first, has the Treasury pay, proves S7a on Holdings whose gas nobody has credited yet, then credits Holdings' gas and pays; every other word of §2 to §4 is kept. (2) SPEC.md §4's signatures — P1 one (the holder's), P2 two (Ben and Cora), P3 three — are Spec 92's signing tiers, which the estate at 8812c64 writes into the compiled charter (`signingTiers`, onboardingcompiler.ts) and enforces nowhere on the payments road: `approvalsRequired` comes from the band and the destination rule, and `routes/approvals.ts` admits the `approver` standing alone, which the charter seats Ada in. The harness walks the spec's road, reports every answer verbatim (Ben and Cora are refused *This action needs an approver*, Ada counts), judges on the four proofs §4 names, and writes both counts in the line. The cure, if Bear wants the tiers enforced, is the estate's.
- Tests: the double learned Spec 104 and Spec 154 (`tests/test_aer360_double.py`): two workspaces on one estate — every `EstateDouble` births a Treasury double beside it on the same platform, chain and step-up secret, and hands over a request the Treasury owns (`owns`: its cookie, its invitation's token, its passkey at the sign-in road); `PlatformDouble`, the gas ledger per account and the admin credit road under `adminAuth`'s three 401 sentences, reason required, an unknown account 404, a repeated key deduped, 201 with the line and the balance; `UsdcChainDouble`, the token contract the estate names, every holder's USDC and the public RPC's `eth_call balanceOf`; `gas_account_preflight` (the fourth gate: the balance before the quotes, the dry run refusing a transfer the key cannot cover as `PAYMENT_UNPRICED`, `insufficient_gas` per quote, U3's set sentence under `GAS_SHORTFALL`, the reservations released at the review's end); `execute_set`/`drive_payment` (the ceiling reserved, the transfer, the debit at actual cost, the instruction confirmed with its `txHash` and `gasValue`, the run settled or partially settled, the trail's `instruction.confirmed` row); `funding_account_balances`, `gas_account`; `requireSourceAccount`'s `WORKSPACE_NOT_PROVISIONED` in the wallet's own sentence (the pre-flight and `asset_short` are gone); `complete_seat_on_charter_write` seating the confirming credential where the charter names an address the estate attributes to it; `PAYMENT_UNPRICED` (502) and `PAYMENT_PRICING_UNAVAILABLE` (503) in the refusal table; a one-off's address stored lower-cased as `payeeaddress.ts` stores one; the Treasury double's float credited only to a wallet that stood before the run (`treasury_funding_wallet="born"`), since Bear funds an address he has seen; the knobs `treasury_usdc_cents`, `holdings_usdc_cents`, `treasury_funding_wallet`, `gas_refusal_names_other_figures`, `review_refuses_but_pays`, `UsdcChainDouble(lose_transfers, fault)`, `PlatformDouble(admin_key, down, debit_gap_cents)`; `runner_on` files the admin credential and mints the Treasury's link unless told otherwise (`admin_env=False`). New: `tests/test_aer360_treasury.py` — each of the spec's tests was red on main: the Treasury pays one payment of US$18.24 to Holdings' address approved with its founder's passkey and lands; the Treasury at US$10.00 fails S7 with the sentence and nothing is submitted; S7a proved with the figures the harness read, a refusal naming other figures fails naming it, a payment that left fails naming it, a balance that covers the ceiling is reported and not failed; the admin credit double receives one credit per workspace with the reason and the run's key, the file absent fails S7 with its sentence, a wrong key is the platform's refusal in its words, a platform down is a fault, the credit is sized by the ceiling; the three payments land with the gas lines and the signers press in the spec's order; a payee whose balance does not rise fails naming the two figures, a balance the RPC could not read is not a pass; S10 reconciles to the cent and a one-cent gap is a finding; the birth run stops with the fund sentence and the rerun pays. The existing tests follow the figures and the new road (`test_aer360_answers`, `test_aer360_double`, `test_aer360_dry_run`, `test_aer360_funding`, `test_aer360_hats`, `test_aer360_roster`, `test_aer360_stations`).
- Not touched: the passkey files' law of T10 (the Treasury's founder passkey is stored and loaded by the same code, under its own folder), the estate, the platform, the gateway; S1 to S6, S8, S9, S11 and S12 make the calls they made before — the frozen dry run proves it, S7's and S10's lines being the only change; corridor_harness.py, series.py, tables.py, aer360_passkey.py and the corridor's tests are not touched.

## Spec T17 — S4 grants a stale seat again before signing, one seat at a time, and S6 counts Ada (23 September 2026)

From the harness run of 21:40 on 22 September against Harness Holdings (report `aer360-harness-2026-09-22-214017.md`) against the estate at AER 360 Specs 99, 100 and 102, and from AER 360 Spec 105 as it is being built (aeredium/AERAccounts, branch `fix/a-seat-tells-the-platforms-truth`: `services/approverseats.ts`, `services/payees.ts`, `packages/shared/src/wire.ts`, `refusals.ts`). The finding, not re-diagnosed: the roster changes list read empty and Ada's seat reported *seated* and current, while the platform refused her whitelist press `SIGNATURE_NOT_COUNTED`, *it does not recognise your key as one of this wallet's signatories*. No ceremony for the move was on record anywhere — Spec 95 proposes a move at a redemption, a grant or a self-seat and at no other moment, so until a founder presses grant again nothing moves. In substance T15 §1 gains a grant step; Spec 105 makes the seat view carry `onRoster`, so the harness can tell a stale seat from a current one and grant it. The harness is the founder and the census at a browser and nothing else.

- `aer360_harness.py`, S4 (`sign_the_roster_changes` now a dispatcher; `_sign_pass_t15`, the T15 pass run unchanged where the seat view has no `onRoster`; `_sign_pass_t17`, `_sign_change_t17`, `_confirm_seat_onroster`, `_record_seat_moved`): after every person has signed in on their own credential, S4 reads `GET /v1/roster/changes` and `GET /v1/approver-seats`. Where the seat view has no `onRoster`, S4 prints *the seat view has no onRoster; skipping* and behaves exactly as T15. Where it carries `onRoster`, for each seat of a person of the harness (matched by email `harness+<name>@aeredium.io`, never by display name) reading `onRoster: false`, taken one at a time: where a change for that seat already awaits, *a change already awaits; not granted*; otherwise the founder grants the seat again through T15's `propose_the_move_afresh` (one call, counting toward `REPROPOSALS_AT_MOST`, and the pass never re-proposes a seat this step granted), and where the grant's answer carries no `pendingTxId` the line says *moved at once* and the signing is skipped. Skipped, each with its line: a seat with `credentialId` null or `ambiguous: true` (*two passkeys enrolled; not granted*); a seat of a person not of the harness (*not of the harness; not granted*); a seat reading `null` (*unverified: <rosterSaid>*). A `true` seat prints nothing.
- `aer360_harness.py`, S4 §2: T15's pass signs the change with two rules added — before signing, the harness signs only where the seat's person is of the harness and the target credential equals that person's stored passkey id, else the finding *change <id> moves <who>'s seat to a credential the harness does not hold; not signed*; and it signs only as persons the change's list names as able (`maySign`), never the stale seat's owner, and — where they have a seat row — reading `onRoster: true` there; a person the list names with no seat row (Ben and Cora on Harness Holdings, seated only Ada under C11 while the whitelist roster carries all three) is able on the list's word alone. Where such persons are fewer than `requiredSignatures`, S4 fails *<n> required, <m> able* and grants no further seat. After the count is met S4 re-reads the seat, up to `SEAT_REREAD_ATTEMPTS` times over a bounded wait, and says *moved* only on an `onRoster: true` read; otherwise it fails naming the seat and *<k> of <n> signatures*.
- `aer360_harness.py`, S6 (unchanged, Spec T17 §3): a `SIGNATURE_NOT_COUNTED` for a person whose seat S4 moved is T15's finding; for a person whose seat read `null` the finding cites the unverified seat; no second press.
- `aer360_harness.py`, S10 (`seat_binding_notes`, Spec T17 §4): one note per seat granted names the ceremony and the signers (`audit_the_trail_for_moved_seats`, unchanged); T12's note *Spec 95 opened a governed ceremony to move it* is deleted from `seat_binding_notes`, because no run recorded a `pendingTxId`, so it was never a fact.
- `aer360_tables.py`: the field's name, `SEAT_ON_ROSTER` (`onRoster`) and `SEAT_ROSTER_SAID` (`rosterSaid`), read from `packages/shared/src/wire.ts` `ApproverSeatView`.
- `aer360_harness.py`, `--dry` and the frozen fixture: under S4 the grant-step read of the seats with `onRoster`, the grant of a stale seat, and the seat read back after the count; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (156 → 159 lines).
- `tests/test_aer360_double.py`: the double learns AER 360 Spec 105 — `seat_on_roster` computes `onRoster` from the live rosters exactly as `rebindRosterSeats`' `moving` filter does (so a seat flips `true` the moment a move applies), and the `seats_override` dial answers `GET /v1/approver-seats` in the Spec-105 shape.
- Tests, each red on main (`tests/test_aer360_roster.py`): a double whose seats view says Ada `onRoster: false`, Ben and Cora `true` and whose changes list is empty has S4 record one grant for Ada's seat before any sign call, sign as Ben and Cora only, and read the seat back `true`; a second view still `false` fails S4 naming Ada with the count; a change already awaiting for Ada is zero grants, the line, and the signing as today; Ada and Ben both `false` on a roster of two fails *2 required, 1 able* after one grant with no second grant; a stranger's, an ambiguous and a `null` seat are zero grants and each line; a change whose target credential is not a stored passkey is not signed, the finding; a grant answering no `pendingTxId` is *moved at once*, no sign call, the seat re-read; and an estate without `onRoster` behaves as T15.
- README: the estate harness section gains the Spec T17 paragraph.
- Not touched, as the fence requires: the passkey files, the estate and the platform.

## Spec T15 — The harness has the approvers sign a change of who the approvers are, and S6 counts Ada (22 September 2026)

From the harness run of 13:50 on 22 September against Harness Holdings (report `aer360-harness-2026-09-22-135010.md`) against the estate before AER 360 Spec 99 (and before Spec 100: the register still read `proposed`), and from AER 360 Spec 99 as it was built (aeredium/AERAccounts, commit 33e039c: `routes/rosterchanges.ts`, `services/approverseats.ts`, `services/changequorum.ts`, `services/audit.ts`, `packages/shared/src/wire.ts`, `enrolment.ts`, `refusals.ts`). The finding, not re-diagnosed: S6 *Ada Approver not counted (SIGNATURE_NOT_COUNTED …)* and S10's note that the platform's roster seat is still bound to a retired passkey — Spec 95 proposed the move when Ada redeemed a fresh invitation, the charter asks two approvers to agree to a change of who the approvers are, the platform holds the move as a pending ceremony, and nobody signed it. With Spec 99 the door exists, and the harness walks through it as the census would. The harness is the founder and the census at a browser and nothing else.

- `aer360_harness.py`, S4 (`sign_the_roster_changes`, `read_roster_changes`, `sign_as_the_list_names`, `sign_roster_change_as`, `assert_the_seat_moved`, `refusal_judged`, `change_words`, `person_named`, `person_by_email`, `route_not_found`): after the seat's re-grant and before the register read, `GET /v1/roster/changes` as the founder. For each change listed awaiting, the harness signs in turn as each person the list names as able to sign — `POST /v1/roster/changes/{pendingTxId}/sign/options` (the challenge the estate derives from `roster-change:<workspace id>:<pendingTxId>:<issuedAtMs>` under `roster.change`) then `POST /v1/roster/changes/{pendingTxId}/sign` with `issuedAtMs` and the assertion, `request(..., retry=False)` — reading `signaturesCollected` after each, until the estate reports it applied; then it reads the roster changes again and asserts the change applied, its seat naming the owner's current credential in the estate's short form (`credentialIdShortForm`, the first eight characters), as the signature's `rebound` named it; the move is recorded in `facts["seats_moved"]` for S6 and S10. A counted signature reports the numbers; a refusal is judged for Rule 13 as S10 judges every refusal (`refusal_without_why`; S10 raises the finding) and reported in the estate's words, never retried, and the next person signs; a person the list names whom the harness holds no session for is reported and passed over. A change listed expired, or approved and unapplied, is reported in the estate's sentence and the move is proposed afresh once (`propose_the_move_afresh`, `REPROPOSALS_AT_MOST`), the list read again and the change signed; a second expiry fails S4 naming the ceremony. An estate before Spec 99, whose roster roads answer route not found, is a note and does not fail S4. Where the register lists no change, S4 says *none awaits a signature: the register lists no roster change, so S4 did nothing new (Spec T15 §4)*. S4 fails where a change it could sign did not end applied.
- `aer360_harness.py`, S6 (`approve_to_quorum`, `seat_moved_for`): the press's expectation says, for a person whose seat S4 moved, that a `SIGNATURE_NOT_COUNTED` is a finding (Spec T15 §2), and one met is raised as a finding of S10's kind in S6, carrying the estate's sentence and the ceremony S4 signed. With every seat on its holder's current credential the loop of Spec T12 counts Ada (1 of 2) and Ben (2 of 2) and stops at the count, so Cora is not asked.
- `aer360_harness.py`, S10 (`audit_the_trail_for_moved_seats`, `seat_binding_notes`): for each seat S4 moved, `GET /v1/export/audit?limit=5000` — the accountant's export, the one road a browser reads the trail on — and a `roster.seat_rebound` row (the estate's verb; SPEC.md spells it `roster.seat.rebound`) with the ceremony's `pendingTxId`, `seatEmail`, `via roster_change` and `signerNames` naming every signer; a row missing, or naming another seat, road or not every signer, is a finding; a row found is a note. Spec T12's note about a seat still bound to a retired passkey is not written for a person whose seat S4 moved — their refused press is S6's finding — and its words no longer say the census "signs it at Spec 99".
- A DISAGREEMENT CARRIED TO BEAR, NOT SILENTLY RESOLVED, in two parts. (1) SPEC.md §1 says that for a change listed expired "the harness signs the seat's owner in once more so Spec 95 proposes it afresh". The estate proposes a move at a re-invitation's redemption, at a seat grant and at the self-seat, and not at a sign-in: `rebindRosterSeats` is called from `routes/auth.ts` (invite/verify) and from the two seat roads in `services/approverseats.ts`, never from login/verify, and `packages/shared/src/enrolment.ts` says in its own words that a sentence telling the person to "sign in again" would send them to a door that proposes nothing; its expired sentence names the two roads that do — *Granting <who>'s seat again in this room, or <who> redeeming a fresh invitation, proposes the move afresh.* The harness takes the lighter of the two, the founder granting the seat again (`POST /v1/approver-seats/grant`), and says so in the step's expectation. (2) SPEC.md covers a change listed expired; the ceremony the live estate holds for Ada's seat was opened on 21 September at Spec 95, before Spec 99 wrote the seat's row (`roster.change_proposed` is written where the awaiting outcome is composed), so tomorrow's list will show it as *a roster change this estate did not propose*, which the estate offers no press for and the harness does not match to a seat by guesswork. It takes the same road once: the founder grants again each seat the charter names for a person of the harness (`facts["seats"]`, Ada's), so Spec 95 proposes any such move afresh and records it — the platform's ceremony is content-bound, so a live ceremony is reached again, now recorded, and a lapsed one, or one whose roster changed since (Ben's and Cora's seats were bound by their presses of 22 September), is born afresh — then reads the list again and signs; a change still unproposed after that is reported and not signed, and does not fail S4. To have the harness only report such a change, drop the `seat is None` road into `propose_the_move_afresh`.
- `aer360_harness.py`, `--dry` and the frozen fixture: under S4 the roster changes read, the options road and the press for each person the list names, the seat granted again for a change listed expired or not proposed here, and the read-back after the count; S6's press lines say what T15 expects; under S10 the trail read; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (150 → 156 lines).
- `aer360_tables.py`: the two roads and the step-up purpose, read from the code — `ROSTER_CHANGES_ROUTE`, `ROSTER_CHANGE_SIGN_ROUTE`, `ROSTER_CHANGE_SIGN_OPTIONS_ROUTE` (the step-up's first half), `ROSTER_CHANGE_PURPOSE` `roster.change` (on `CHANGE_CEREMONIES`, C12C's family), `ROSTER_CHANGE_BINDING` `roster-change:%s:%s:%s`, `ROSTER_CHANGE_OPERATION` `multisig_update`, `ROSTER_CHANGE_STATES`, the trail's verbs `ROSTER_SEAT_REBOUND` and `ROSTER_CHANGE_PROPOSED`, `VIA_ROSTER_CHANGE`, `AUDIT_EXPORT_ROUTE` and `AUDIT_EXPORT_LIMIT`, and `credential_short_form`. `aer360_harness.py` carries the codes the pass judges: `CHANGE_SIGNER_NOT_ON_ROSTER` (403), `ROSTER_CHANGE_UNKNOWN` (404), `APPROVER_ALREADY_SIGNED` (409), `SIGNATURE_NOT_COUNTED` (403), `PLATFORM_REFUSED` (502).
- `tests/test_aer360_double.py`: the double learns AER 360 Spec 99 and, beneath it, the platform's pending-transaction roads as the estate's own double models them (`apps/server/src/test/aapDouble.ts`). Spec 95's rebind (`rebind_roster_seats`, `replace_multisig_signers`) at a re-invitation's redemption (answered as `rosterSeats`) and at a seat grant — never at a sign-in; the multisig_mutation roster seated with the whitelist roster at the first compile (`seat_change_roster`, `governanceSignersFor`: the census at C12's count); the ceremony's lifecycle (`born_ceremony`, `ceremony_is_live`: pending, approved at the count, consumed when the change is presented again with the same content, the lapse read off `expires_at`); `roster.change_proposed` recorded against the seat; `GET /v1/roster/changes` (`list_roster_changes`, `roster_change_view`, `roster_change_state`, `caller_seat`, `addresses_of_caller`, `register_names`, `seat_name`, `signer_view`), the options road and the signature (`roster_change_sign_options`, `sign_roster_change`, `apply_roster_change`) in the estate's fail-closed order — `ROSTER_CHANGE_UNKNOWN`, `CHANGE_SIGNER_NOT_ON_ROSTER` in the roster's words before the platform is asked, `APPROVER_ALREADY_SIGNED` from the platform's record, the platform's *conflict: pending transaction expired* relayed as `PLATFORM_REFUSED`, *not authorized* as `SIGNATURE_NOT_COUNTED* — the count met, the change presented again, the seat moved and the `roster.seat_rebound` row naming the ceremony and every signer; every sentence word for word (`roster_change_sentence`, `roster_change_signed_sentence`, `not_on_change_roster_sentence`, `platform_refused_sentence`, `roster_seat_awaiting_sentence`, `when_in_words`); `GET /v1/export/audit` (`audit_export`, `append_trail`); and the whitelist press the platform no longer needs, *entry is already active*, relayed as `ADDRESS_PROPOSAL_REFUSED` in the approve road's own sentence (`approvalPlatformRefusal`). The platform's credential ids are UUIDs here now, so their short form tells them apart. The dials: `change_roster=("ben", "cora")` (the census keys seated on the multisig_mutation roster; None seats the compiler's four), `before_spec_99` (the estate at Spec 95: the move proposed, no seat row, no roster roads), `ceremony_lapses` (the next ceremonies born lapse at birth), `lapse_after_first_signature` (the clock passes the expiry after the first signature), `account_email` (the AAP account's own address, attributed to the founder's key; None by default, so the founder's key is attributed no census seat and the founder is refused in the roster's words).
- Tests, each red on main (`tests/test_aer360_roster.py`; `tests/test_aer360_stations.py`; `tests/test_aer360_double.py`; `tests/test_aer360_dry_run.py`): the spec's double — one awaiting change for Ada needing 2, Ben and Cora able to sign — has S4 sign as Ben (1 of 2) and as Cora (applied), assert the seat and report both, with the list's and the signatures' sentences the estate's own; the platform refusing Cora's signature, the second of the ceremony, in its own words (*conflict: pending transaction expired*): reported, not retried, S4 failing and S6's refusal a note; a second signature by the same person refused from the platform's record, which the harness never makes; the double answering expired: the seat granted again, the change re-listed and signed; a second expiry: S4 fails naming the ceremony; S6 with every seat current: Ada 1 of 2, Ben 2 of 2, Cora not asked and refused as unneeded in the platform's words when pressed; S10 finds the trail row per moved seat, and a trail without it, or with a row naming another road or not every signer, is the finding; the estate before Spec 99 (the door not open, S4 says so) and the day after (the ceremony of Spec 95 listed as one this estate did not propose, the grant proposing the move afresh, the fresh change signed); a fresh estate unchanged but for one line; the double's refusals against Spec 99's own tests (a viewer refused in the roster's words before the platform is asked, a foreign ceremony unsignable here, a step-up of another purpose refused, the platform's own no); the four-at-two of Spec T10 now signed by the census and counted in S6; `--dry`.
- README: the estate harness section gains the Spec T15 paragraph.
- Not touched: `aer360_answers.py`, the passkey files, the estate; `corridor_harness.py`, `series.py`, `tables.py`, `aer360_passkey.py` and the corridor's tests.

## Spec T13 — The harness presses for the funding wallet, funds it from the faucet as the founder would, and reads the payee register back after the count (22 September 2026)

From the harness run of 23:44 on 21 September against Harness Holdings (report `aer360-harness-2026-09-21-234438.md`) against the estate at AER 360 Spec 98, and from the faucet's mechanism as Bear recorded it the same night (the Filing Cabinet, Deployments drawer, *AEREDIUM testnet faucet, the auto-pay mechanism end to end, 21 September 2026*). Two findings, neither re-diagnosed. S7: no funding wallet was born — Spec 98 births it inside the wallet account's write, and this estate's account was an amendment with nothing new to write — so every payment met *No funding account has been set for this workspace, so network fees cannot be checked.* and S8 read `transactable False, reason no funding wallet`, which is the truth; the founder's press exists (`POST /v1/workspace/funding-wallet`, author, mutating, a step-up whose challenge is derived from `funding-wallet:<workspace id>:<issuedAtMs>` under `FUNDING_WALLET_PURPOSE`; `GET /v1/workspace` answers `fundingWallet` or `fundingWalletAbsence`). S6: the estate's register lagged the platform — `proposed` after the second count (Spec 100 fixes the mirror; the register corrects itself on the next `GET /v1/payees`) — and the harness judged the press's answer. The harness is the founder at a browser and nothing else; the faucet page is a page a founder on the testnet would use.

- `aer360_harness.py`, S5 (`give_the_estate_its_funding_wallet`, `funding_wallet_words`): after the wallet account's read-back is confirmed and compiled, `GET /v1/workspace`. Where `fundingWalletAbsence` stands, the press as Harriet: `POST /v1/workspace/funding-wallet/options` — the challenge the estate derives from `funding-wallet:<workspace id>:<issuedAtMs>` under `FUNDING_WALLET_PURPOSE`, `workspace.funding_wallet` — then `POST /v1/workspace/funding-wallet` with `issuedAtMs` and the passkey's assertion, once and never retried (`request(..., retry=False)`: an assertion cannot be sent twice, so a 5xx on the press is reported as the estate said it), then `GET /v1/workspace` again and the line *funding wallet: <address> on <home stack>, key <id>*. A refusal is reported in the estate's words — `WALLET_BIRTH_REFUSED` with the gateway's sentence carried in `detail.gatewaySaid`, `GATEWAY_UNAVAILABLE` a fault — and S5 fails naming it. A wallet already born is reported and not pressed for again (`FUNDING_WALLET_ALREADY_BORN` is never met, because the harness reads before it presses). An estate before Spec 98, whose workspace answers neither field, is reported in a note and not failed. A read-back after the press that does not carry the wallet is a finding of S10's kind, raised in S5.
- `aer360_harness.py`, S5 (`read_native_balance`, `fund_the_wallet_from_the_faucet`, `faucet_words`, `request_outside`, `outside_sentence`): the estate offers a browser no live balance of its funding wallet (no route; `GET /v1/aer360/wallets` is the daily close's record), so the wallet's native balance is read with `eth_getBalance` from the chain's public RPC named in the faucet record. Below 0.1 SEAR (`GAS_FLOOR_SEAR`) the harness POSTs the address to `https://aeredium.io/faucet-api/request` as JSON `{"address": "<address>"}`, exactly as the faucet page does, once per run (`facts["faucet"]` guards a second ask), and reports the answer verbatim — *the faucet paid: paid true, tx_hash <hash>*; *the faucet refused (HTTP 429): <its sentence>*; a request saved without paying; a faucet that could not be reached. A balance that could not be read is reported in the RPC's own words and the faucet is not asked: the harness never invents a balance. A call to the RPC or the faucet is `Call.outside` — recorded in the evidence and in Every call, measured by S12, never retried, and never judged as the estate's: `audit_money` and `audit_refusals` skip it, and so does S10's count of the refusals met. A road that cannot be reached is recorded as a fault, status 0 and the fault's words, and never fails the station: the estate is not the one that failed.
- `aer360_harness.py`, S6 (`station_s6`, `approve_to_quorum`, `mirror_sentence`, `MIRROR_DISAGREES`): the register is the judge, not the press. Each press that counts records the count the platform reached (`record["count"]`); after the presses `GET /v1/payees` decides each payee on its `whitelistStatus`; S6 passes when both read `whitelisted`, and a register reading `proposed` after the platform counted 2 of 2 fails with *the platform counted 2 of 2 and the register reads proposed; the estate's mirror disagrees with the platform* — the truth until Spec 100 ships, a pass after it.
- `aer360_harness.py`, S7 (`payments_need`, `payments_need_line`): with a funding wallet present and a payment refused at creation, the refusal stands verbatim and one line says what the three payments need together — *funding wallet <address>: the three payments together need US$18,249.99 of USDC on ethereum (1250.00 + 4999.99 + 12000.00 = 18249.99 USDC) — the harness never mints or moves the asset; fund it by hand* — in the station's line and as a note. The workspace step reads `fundingWallet` (so a run resumed past S5 still names the wallet), and the no-funding-account note carries the estate's absence sentence and points at S5's press.
- `aer360_harness.py`, S8 and the report (`funding_summary_lines`): S8's line names the funding wallet beside the readiness it decides — `transactable True` once the wallet exists, `False, reason no funding wallet` before it; the report's summary under the closing table carries the wallet's address with its explorer link, the gas read and the faucet's answer, and the asset the payments need, so all three can be checked by eye. `read_report` reads the closing table as before.
- `aer360_harness.py`, `--dry` and the frozen fixture: S5 gains the workspace read, the options road, the press, the read-back, the balance read and the one faucet call, each saying what it expects; S6's register line, S7's workspace line and S8's readiness line say what is judged; S7 gains the `[report]` line for the asset; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (143 → 150 lines).
- `aer360_tables.py`: the testnet and the faucet as facts read from the cabinet's faucet record and the faucet page's own network helper, never invented — `TESTNET_CHAIN_ID` 2237 (0x8bd), `TESTNET_NAME`, `NATIVE_COIN` SEAR, `TESTNET_RPC_URL` `https://testnet.rpc.aeredium.io` (read back on 22 September 2026: `eth_chainId` answered `0x8bd`), `TESTNET_EXPLORER_URL`, `FAUCET_PAGE_URL`, `FAUCET_REQUEST_URL`, `FAUCET_PAYS_SEAR` 0.5, `FAUCET_LIMITS`, `GAS_FLOOR_SEAR` 0.1 and `GAS_FLOOR_WEI`; `to_wei`, `coin_amount` and `payments_total` by string arithmetic, no float anywhere. No address of the faucet's or the treasury's is written here.
- `tests/test_aer360_double.py`: the double learns AER 360 Spec 98 (aeredium/AERAccounts, commit 56582b8; `routes/workspace.ts`, `services/fundingwallet.ts`, `services/stepup.ts`), read from the code — `GET /v1/workspace` answers `fundingWallet` `{address, keyId, homeStack, bornAt, sentence}` or `fundingWalletAbsence`; `POST /v1/workspace/funding-wallet/options` and the press (`funding_wallet_options`, `funding_wallet_press`, `birth_funding_wallet`, `funding_wallet_binding`) derive the challenge from the binding `funding-wallet:<workspace id>:<issuedAtMs>` | credential | `workspace.funding_wallet` as `deriveChallenge` binds it and verify the assertion against it; readiness reads `transactable false, reason no funding wallet` until both columns stand; `FUNDING_WALLET_ALREADY_BORN` (409), `WALLET_BIRTH_REFUSED` (502, the gateway's words of 21 September in `detail.gatewaySaid`) and `GATEWAY_UNAVAILABLE` (503, named with its setting) in the estate's sentences. The dials: `funding_wallet="press"|"born"|"refused"|"unavailable"` (the typed `source_account` is gone — Spec 98 closed the typed-address road, and a funding wallet is both columns); `mirror_lags` (the press that meets the count answers the row's stale `proposed`) and `register_corrects` (Spec 100: the register reads `whitelisted` on the next read); `asset_short` (gate 4, the gas pre-flight's estimate of the USDC transfer reverts at the RPC, `GAS_PREFLIGHT_UNAVAILABLE` with the RPC's words as the cause). Beside it `FaucetDouble` — the faucet as its own handler answers (aeredium/faucet, `internal/faucet/server_manual.go`: `ok`, `paid` and `tx_hash`; `ok` and `queued`; its refusal sentences under `error` at 400, 409 and 429) — and `TestnetRpcDouble`, the chain's RPC for `eth_getBalance` with its faults; the estate double routes each host to its own double.
- Tests, each red on main (`tests/test_aer360_funding.py`; `tests/test_aer360_stations.py`; `tests/test_aer360_dry_run.py`): a double answering `fundingWalletAbsence` has the harness press with a step-up whose binding is `funding-wallet:<id>:<ms>` under the purpose, read the wallet back and report the line, with the press made once; a double answering a wallet already makes no press; a faucet answering `paid` with a `tx_hash` puts both in the line and the summary, a refusal puts the faucet's own sentence, a saved request and an unreachable faucet are reported, and the harness asks once per run; a balance at or above 0.1 SEAR asks nothing, and a balance that could not be read (unreachable, a JSON-RPC error, HTML) asks nothing and is reported; a register reading `whitelisted` after the count passes S6 though the press answered `proposed`, and one reading `proposed` after 2 of 2 fails with the mirror sentence; S7 with a wallet present and the payments refused for the asset names the address, the asset and US$18,249.99; the estate of 23:44 (the gateway refuses) fails S5 naming `WALLET_BIRTH_REFUSED` with the gateway's sentence, S7 in the route's words with no asset line, and S8 reads `transactable False, reason no funding wallet`, the hats still running; the outside calls raise nothing under the estate's audits; the tables' facts and arithmetic; `--dry`.
- README: the estate harness section gains the Spec T13 paragraph.
- Not touched: `aer360_answers.py`, the passkey files, the estate, the faucet; `corridor_harness.py`, `series.py`, `tables.py`, `aer360_passkey.py` and the corridor's tests.

## Spec T12 — The harness follows a walk-back as a browser would, and S6 presses the roster people whose presses count (21 September 2026)

From the harness run of 22:31 the same night against Harness Holdings (report `aer360-harness-2026-09-21-223148.md`) against the estate at AER 360 Spec 95: three findings, each the harness reporting a failure where a founder's browser would have carried on. S5 met the wallet-account read-back's `CHARTER_INCOMPLETE` — *"Three signatures are asked above US$10,000 and 00 cents but only 2 people are named; name more or lower the tiers."*, `walkBackTo {questionId: "WA1"}`, HTTP 409 — on a resumed draft (`721822d0`) whose WA1 an earlier run had answered with Ada alone, and stopped. S6 pressed Ada, whose platform seat is bound to her retired credential …ddb1 (Spec 95 opened a governed ceremony to move it; the census signs it at Spec 99), met `SIGNATURE_NOT_COUNTED`, and moved to the next payee without pressing Ben or Cora, whose seats bind and count. S10 carried nineteen findings comparing the resumed read-back and charter only against the ten answers this run sent, so every earlier answer became a finding. The estate is right in all three; the harness is the founder at a browser and nothing else, and it changes nothing at the estate.

- `aer360_harness.py`, S10 (`audit_readback`, `audit_charter`, `station_s10`): the read-back and charter comparisons take their expectation from the answer book (`ANSWERS`) for every question the read-back carries, whether or not this run answered it — a resumed interview's read-back carries the answers earlier runs gave, and the book is what they were, so they are no longer findings. A line the book does not know is a finding as before. `audit_readback` compares line by line, rendering each from the run's answer where it answered, else the book (`kind_of`); the C19 door line reads its answer from the run or the book alike. The provenance note (`readback_provenance_note`) says how many lines came from this run's answers and how many from the book.
- `aer360_harness.py`, the interview walk (`confirm_and_compile`, `walk_back_of`, `follow_walk_back`, `drive_to_read_back`): a walk-back is followed, once per question. Where the read-back (`GET`) or a confirm answers `CHARTER_INCOMPLETE` with `walkBackTo`, the harness answers the named question from the book, resumes to the read-back and reads it again, and reports a note — *walked back to WA1: <the estate's sentence>; answered from the book and returned to the read-back*. The same question walked back to twice is a failure carrying both sentences (a book that cannot satisfy the belt is reported, never looped); a question the book does not know is a failure that names it. This applies to S3 and S5 alike.
- `aer360_harness.py`, S6 (`approve_to_quorum`, `whitelist_pressers`, `station_s6`): the harness presses the roster people it holds a passkey for, in the charter's roster order with the founder last, on their current credential, until the estate answers `whitelisted` or nobody is left. Each press is recorded with the estate's answer verbatim; a press refused `SIGNATURE_NOT_COUNTED` is recorded and the next person presses. S6 passes when both payees are whitelisted, and its line names who counted and who did not. `next_presser` and the pre-89 "one further press" special-casing are gone; the first pending press that counts is still audited (Spec T9 §2). Harness Holdings tonight: Ada refused, Ben and Cora counted, two of four.
- `aer360_harness.py`, S10 (`seat_binding_notes`): for each roster person whose press was refused `SIGNATURE_NOT_COUNTED`, a note names the credential the harness pressed with and the one the People register says they hold, so the report shows the seat still bound to a retired passkey until Spec 99 lands. A note, not a finding: the estate is telling the truth.
- `aer360_harness.py`, S7: unchanged (Spec T12 §4). It still waits on Spec 98's funding wallet on the live estate and says so; on the double it now sees the payees S6 whitelisted, so P1 to a whitelisted Northwind clears at submission (status approved, approvalsRequired 0), and Ada's approval of it is not asked.
- `aer360_harness.py`, `--dry` and the frozen fixture: S6's two presses (Ada, then the next roster member) become the roster press — one line per roster presser (Ada, Ben, Cora), the founder last — each *count it toward the quorum … or refuse SIGNATURE_NOT_COUNTED … the harness presses on until whitelisted or nobody is left*; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (141 → 143 lines).
- `tests/test_aer360_double.py`: the whitelist door learns AER 360 Spec 95 — `address_action`'s approve and reject admit any authenticated author (a viewer is still refused), and the platform's ceremony (`sign_whitelist_ceremony`) is the sole judge, binding and counting an active seat or refusing `SIGNATURE_NOT_COUNTED`; the seated-approver requirement and the submitter-may-not-approve gate are dropped from the whitelist road.
- Tests, each red on main: a read-back fixture of twenty-eight lines of which the run answered ten raises no finding and the note says ten from the run and eighteen from the book; a double whose read-back walks back to WA1 once has the harness answer WA1 from the book, return, pass and write the note, and the same walk-back twice is a failure with both sentences (`WalkBackDouble`, `TheHarnessFollowsAWalkBack`); the four-at-two double (Ada's seat bound to the retired credential, Ben's and Cora's never bound) whitelists both payees and S6's line names all three; a double whose roster seats nobody refuses every press and S6 fails naming each; S6's fresh-estate, before-Spec-89, roster-below-quorum and never-activates doubles are pressed as the roster, not as Ada-then-Ben; S7's P1 clears at submission now that Northwind is whitelisted; and `--dry` carries the roster press.
- README: the estate harness section gains the Spec T12 paragraph.
- Not touched: `aer360_answers.py` (T11's book stands), the passkey files under `~/.aer360-harness`, the estate, and `corridor_harness.py`, `series.py`, `tables.py`, `aer360_passkey.py`, `aer360_tables.py` and the corridor's tests.

## Spec T11 — The answer book learns catalog version 14, and the auditor reads the new law back (21 September 2026)

From the harness run of the same night against Harness Holdings (report `aer360-harness-2026-09-21.md`): S3 and S5 stopped — *the answer book has no answer for policy question C11A* and *… for wallet_account question WO1*. AER 360 Spec 92 (aeredium/AERAccounts, commit cf3be4a; `questioncatalog.ts`, `CATALOG_VERSION 14`; `onboardingcompiler.ts`; `services/onboarding.ts`; `services/payees.ts`; `packages/shared/src/venues.ts`, `refusals.ts`) gave the catalog seven questions the book did not know — C11A, C11C, C19, WO1, WO2, WO3, WO4 — and one new answer kind, `person_or_none`. With the interviews unfinished, S10 made no read-back and no charter comparison, and S11's draft probes were not made. The harness is the founder at a browser and nothing else; it changes nothing at the estate.

- `aer360_answers.py`: the book answers the seven, in the catalog's own words. `C11A` `{"choice": "The people who may change these rules, at the number you set for a change"}` — the census at C12's count of two, the roster Spec T10 asserts in S6; `C11C` `{"entries": []}`, not served behind that answer and written all the same so a walk that reaches it is answered and reported rather than stopped; `C19` `{"choice": "No — only wallets held by people or companies"}`; `WO1` `{"choice": "One person, named here", "person": {"name": "Ben Signatory", "email": <Ben's>}}` — Ben holds the wallet and is its Officer; `WO2` `{"entries": [{"name": "Harriet", "surname": "Founder", "email": <Harriet's>, "title": "CEO"}]}`; `WO3` `{"cents": "200000"}` (2,000.00, the holder alone) and `WO4` `{"cents": "1000000"}` (10,000.00, two signatures enough), both in `MONEY` as `holder_alone_cents` and `two_signatures_cents`. `WA1` gains Ben beside Ada, so the tiers name three people (Ada, Ben, Harriet) and the read-back is not refused; `WQ` stays at one. The catalog gains the seven entries with their gates (C11C behind C11A's third answer; WO1, WO2 and WO4 behind every purpose but the agents'; WO3 behind WO1's first answer) and W1's new option, the customer's account; `SHAPE_OF_KIND` learns `person_or_none`; `CATALOG_VERSION_ANSWERED = 14`, `CATALOG_SOURCE` at cf3be4a, `ADDED_IN_V14`, `WALLET_HOLDER`, `WALLET_HOLDER_TITLE`, `THIRD_PARTY`, `THIRD_PARTY_TITLES`, `WRITTEN_ONE_DOLLAR`, and the option constants `PAYEE_APPROVAL_*`, `VENUE_YES`/`VENUE_NO`, `HOLDER_PERSON`/`HOLDER_NO_ONE`, `CUSTOMER`, `NON_AGENT_PURPOSES`.
- `aer360_harness.py`, S3: reports the book's version beside the estate's — *the answer book answers catalog version 14, and the estate served 23 question(s), every one known to the book* — and says that the estate states no catalog version on the roads a browser walks (`routes/onboarding.ts` answers an interview's id and state). A question the estate serves that the book does not know is still reported as today, with the version pair in the sentence: the book's version, and the inference that the estate's catalog is later than 14 or is not the one the book read.
- `aer360_harness.py`, S10 (`audit_charter`): five new expectations beside Spec T8's — *who approves a new payee (C11A: the answer, its roster and its quorum)*: `payeeApproval` `{answer: change_approvers, roster: the census of four as "Name <email>", quorum: 2}`, read as `payeeApprovalOf` reads each of the three answers; *may a payee be a venue's contract (C19)*: `payeeVenueContracts` `refused`; *who holds this wallet (WO1)*: `holder` `{held: by_person, name: Ben Signatory, email, title: Officer}` (`holderTitleFor`), or `{held: by_no_one}`; *the holder's own figure (WO3)*: `signingTiers.holderAloneUpToCents` `200000`; *the two-signature figure (WO4)*: `signingTiers.twoSignaturesUpToCents` `1000000`. The signers expectation learns that WO2's third party is a signer beside WA1's people, once by email (*the signers (WA1, and WO2's third party once)*). `spoken_for` learns `person_or_none` (*<name> — <email>.*) and a written zero (*… — nothing is paid under this rule until you write a figure.*), read from `spokenAnswer` at cf3be4a, so no kind the catalog serves goes uncompared. `audit_readback` says, where a money line still speaks *US$1 and 00 cents.*, that the read-back speaks the figure the field arrived written with and not the one the book wrote; and where C19 is No it expects the door's own line, `C19_DOOR`, *You answered No: such an address will be refused when entered.*
- `aer360_harness.py`, S11 (`probe_venue_contract`, `venue_law`): the venue probe follows the charter. It reads the compiled policy charter's `payeeVenueContracts` at run time: under `refused` it expects `PAYEE_IS_VENUE_CONTRACT`, 422, with the estate's sentence (`payee_is_venue_contract_sentence`, `payeeIsVenueContractSentence` word for word: *This address is the contract of Uniswap v3 on ethereum. Your charter says a payee must be a wallet held by a person or a company (question C19). Nothing was saved.*, the venue named as `packages/shared/src/venues.ts` names it), and reports an acceptance as the finding — *ACCEPTED: HTTP 201 — the charter answered No at C19 (payeeVenueContracts refused), so the payee door must refuse PAYEE_IS_VENUE_CONTRACT, 422; it saved a venue's contract*; a refusal under another code or another sentence is a finding carrying the estate's words, a refusal without its why a finding as before. Under `accepted`, or a charter never asked (null), the expectation of 20 September stands, with where it was read beside the ruling. A run that compiled no policy charter reads the book's C19 and notes it. `venue_law_of`, `venue_law_of_the_book`; the constants `PAYEE_IS_VENUE_CONTRACT`, `PAYEE_IS_VENUE_CONTRACT_STATUS`, `VENUE_CONTRACT_QUESTION_ID`, `ESTATE_VENUE_NAMES`, `CORRIDOR_VENUE_IDS`.
- `aer360_harness.py`, S7 (`tiers`, `under_the_tiers`, `usd`): the three payments keep their amounts, and the expectation column — the review and submit steps, and the station's line — says what the tiers would do with each: P1 (1,250.00) *within the holder's own figure (US$2,000.00), one signature — the holder's*; P2 (4,999.99) *two signatures (above US$2,000.00, up to US$10,000.00)*; P3 (12,000.00) *three signatures (above US$10,000.00)*, the figures read from this run's compiled account charter's `signingTiers`, else the book. S7 cannot run until the workspace has a funding account, which the estate gives no browser control for and which waits on the wallet's birth (Specs 93 and 147, unwritten); the harness keeps saying that in its one line and does not invent a source account.
- `aer360_harness.py`, `--dry` and the frozen fixture: the six answers a version-14 estate serves this book (C11A and C19 under S3; WO1 to WO4 under S5), one S10 comparison line for the Spec 92 fields, the venue line's expectation read off the book's C19, the S7 submit lines' tier words, and the S3 first line's version pair; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (134 → 141 lines).
- New: `tests/fixtures/aer360-served-2026-09-21.json` — every question page the estate served the harness on 21 September 2026 (14 policy pages to C11A, 13 account pages to WO1, copied from the report verbatim) and the seven questions version 14 added in the catalog's own words, with their gates, list fields, notes and the written dollar.
- Tests: the double learns Spec 92 (`tests/test_aer360_double.py`): it serves the seven from the fixture (`PROMPTS`), WO3 and WO4 with the written dollar as their value where no answer stands; validates `person_or_none` as `validateValue` does; speaks a person and a written zero as `spokenAnswer` does and carries Spec 92's synthetic lines (`C19_DOOR`, `WO1_TITLE`, `HOLD_NOT_WRITTEN`, `WQ_TIERS`); refuses at the read-back, the confirm and the compile where WO4 is not above WO3 (`TIER_TWO_NOT_ABOVE_ONE`) or fewer than three people are named between WA1 and WO2 (`tiers_need_three_people`), in the spec's sentences; compiles `payeeApproval` (`payee_approval_of`), `payeeVenueContracts` (`venue_contracts_of`), `holder` and `signingTiers` (`read_wallet_people`, with its refusals) and the third party among the signers; seats the whitelist roster the answer to C11A draws up; and at the payee door refuses a known venue's contract by name — `PAYEE_IS_VENUE_CONTRACT`, 422, `payee_is_venue_contract_sentence` — where the written policy charter says `refused`, from the engine's venue table copied row for row (`VENUE_CONTRACTS`, `VENUE_NAMES`). Its venue dial is redrawn: `refuses_venue_contract=None` (the default) follows the charter; `True` is Spec T8's stand-in, a door refusing regardless under `ADDRESS_PROPOSAL_REFUSED`; `False` a door saving regardless; `catalog_version=12` leaves the seven out. Each of the spec's tests was red on main: the book answers every question the catalog at version 14 serves for the two interviews of Harness Holdings (the fixture of served ids plus the seven; no id missing); WO1 decodes to the estate's `person_or_none` shape, WO2's title is one of the three, WO4 is above WO3; S10's five expectations pass against a charter fixture written from Spec 92's compiler types and fail against the fixture of 21 September (no `payeeApproval`, no `holder`); the venue probe expects refusal when the charter fixture says `refused` and acceptance when it says `accepted`; and against the double the founder's road walks 23 and 18 questions, S10 raises nothing new, S11 meets the door's refusal by name and raises no venue finding, a door that saves regardless is the finding, and an estate whose C19 is Yes keeps the expectation of 20 September.
- README: the estate harness section says what the book answers, what S10 reads back, what the venue probe expects and what S7 says of the tiers.
- Not touched: the estate, the passkey files under `~/.aer360-harness`, and the corridor harness. S1 to S6 make the calls they made before — the frozen dry run proves it: the six answers a version-14 estate serves are the only new calls — and the one change on the interview road is the sentence an unknown question stops with, which now carries the version pair; S6's press loop and every figure of the payments are as they were. corridor_harness.py, series.py, tables.py, aer360_passkey.py, aer360_tables.py and the corridor's tests are not touched.

## Spec T10 — Every person is brought in on their own credential, and the harness proves the quorum (20 September 2026)

From the fourth live run against Harness Holdings (report `aer360-harness-2026-09-20-135419.md`, station S6): Ben's press after Ada's was counted as Ada's, because the two shared one credential. AER 360 Spec 91 (aeredium/AERAccounts, commit 9964205, PR #112) then made an `author` invitation mint a credential of the person's own on the account's role-bearing policy entry, bound to the passkey on redemption; it left a passkey enrolled before it speaking for the shared credential until that credential is revoked at the platform, and a seat bound to the shared credential bound until it is re-granted. The harness's three people were enrolled before Spec 91, and Ada's seat was granted before it.

- `aer360_harness.py`, S4: reads Ada's seat as the run finds it; after each person signs in with their stored passkey, compares the session's `credentialId` with the founder's. Equal is the shared credential of S10's finding: S4 says so and brings the person in again — the founder mints a new `author` invitation for the same name and email (`mint_invite`), the person redeems it with a NEW software passkey (`enrol_by_invite`) stored beside the old one as `<person>-2-<date>.json` and never over it (`next_key_path`), and the session is the new one. The step's expected line is *a credential of <name>'s own, not the founder's*; the result names the last four characters of both. Different is *already on their own credential*, and nothing is minted. A fresh enrolment that lands on the shared credential (an estate before Spec 91) is reported and not re-invited in the same run. A refusal anywhere on the road is told in the estate's words, and the person keeps the session and the passkey they had.
- `aer360_harness.py`, S4: Ada's seat is granted again (`POST /v1/approver-seats/grant`) where it is `enrolled_not_seated` or names a credential other than her session's; the line says which credential the seat named as found, after the people were brought in, and after the grant; a grant that does not name her own credential, or one the estate refuses, is a finding in the estate's words, and S4 fails.
- `aer360_harness.py`, the store: `load_passkeys` signs each person in with the newest passkey stored for them at this base and leaves older ones alone; `save_passkey` writes a passkey back to the file it came from (`Person.key_file`) and never to another key's; `stored_key_paths`, `next_key_path`, `retired_passkeys`. `aer360_passkey.py`: `stored_key_paths` and `next_key_path` name a person's key files, oldest first, `<stem>-<n>-<date>.json` beside `<stem>.json`.
- `aer360_harness.py`, `audit_people` (S10): asserts, after S4, that the sessions carry distinct credential ids and none but the founder's is the founder's, and says so as a note — so the closing table's last-run column says *closed* for *one credential for several people*; the finding, where it stands, names the founder's credential. Reads the register's Spec 91 marker (`sharesCredentialWith`, spoken as the People screen speaks it: `shares_credential_sentence`, from `packages/shared/src/enrolment.ts`) for each of the harness's people: expected absent after the re-invitation, a finding where it stands, quoting the register and the screen's sentence. The founder's row is the one marker the harness expects: the passkeys it retired still speak for the founder's credential at the estate, which does nothing to them by itself, so a marker naming exactly their labels is a note; any other name is a finding. A register without the field (an estate before Spec 91) is a note.
- `aer360_harness.py`, `--dry` and the frozen fixture: under S4, the seats as the run finds them, then for each author the comparison and the conditional re-invitation (invites, invite/options, invite/verify with a NEW passkey stored beside the old), the seat re-grant's condition, and the register read; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (121 → 134 lines).
- Tests: the double learns Spec 91 (`tests/test_aer360_double.py`): an author invitation mints a credential of the person's own on the one policy entry bearing authorship (several are `INVITE_INVALID`, listing entries and credentials); redemption binds the passkey and retires the bindings an earlier invitation of the same address enrolled (`person.credential_replaced`); `GET /v1/invites` marks each person's newest redeemed row with the other labels whose passkeys speak for its credential; a shared credential is refused a seat by name (`APPROVER_SEAT_CREDENTIAL_SHARED`, 409, the sentence composed as `approverSeatSharedSentence` composes it) and is never seated by the redemption road; several credentials on one address are `APPROVER_SEAT_AMBIGUOUS` (409); the change governance is established once and a recompile leaves the rosters standing (`governanceAlreadyStood`); and a ceremony press is counted by the platform's rule, established by Spec 91's builder from `validateMultisigSigner` — matched by credential first, an empty seat bound by the address the estate's press names, a seat bound to another key refused *not authorized* — which corrects Spec T9's own choice of matching by email. Knobs: `before_spec_91` (the estate of the four live runs), `seat_completes_on_redemption=False` (before Spec 58's count 3), `second_authorship_entry`, `platform_names_approver` (an operator at the platform's console naming a minted credential a second approver). Each of the spec's tests was red on main: against the pre-91 shape with four people on one credential, S4 re-invites three, stores three new passkeys with the suffix, the seat names Ada's new credential (seated on redemption; the grant road proven where the redemption seats nobody), the sessions carry four distinct credentials and S10 raises no credential finding, closing the old one in the last-run column; where the credentials already differ nothing is minted and each person keeps their one passkey; a seat grant refused is a finding with the estate's sentence; the old passkey files differ after the re-invitation only in the counter the one sign-in moved, and a run after it leaves them byte for byte; `--dry` carries the conditional lines.
- A DISAGREEMENT CARRIED TO BEAR, NOT SILENTLY RESOLVED: SPEC.md §4 says S6 "now proves the quorum" — `whitelisted` after Ada's press and Ben's. The estate's code at 9964205 says otherwise for this charter, and the double refuses what the estate refuses. First, `POST /v1/payees/addresses/:id/approve` requires the `approver` standing (`routes/payees.ts:221`), which `evaluateRoles` (`services/roles.ts`) grants only to a credential the policy names as a second approver, and the charter seats Ada alone (C11, WA1): Ben pressed as an approver until now only because he wore the founder's seated credential; on a credential of his own he is refused at the guard — *This action needs an approver* — while the compiler's `whitelist_mutation` roster still names him (Spec 89; `routes/guards.ts` itself says the two are "two different rosters answering two different questions"). Second, the change governance is established once (`governanceAlreadyStood: true` on the live recompile receipt), so the roster's seat for Ada is still bound to the credential her first press wore, the founder's, and the platform refuses her own credential — *not authorized* (`test/aapDouble.ts`, the signatures road: "a seat already bound to a different key refuses") — relayed as `SIGNATURE_NOT_COUNTED`. So on the rerun S4 will bring the three in again and S10's finding will close, and S6 will report those two refusals verbatim, as the ship note asks. The answers to the interviews are fenced, so the harness cannot seat Ben; the cure is the estate's (a guard that admits the roster's members, and a seat rebound when a credential is replaced) and is Bear's to decide.
- Not touched: S6's press loop, the founder's enrolment, the answers to every interview and every figure; corridor_harness.py, series.py, tables.py, aer360_tables.py and the corridor's tests are not touched.

## Spec T9 — A payee is whitelisted when its quorum is met, and the harness presses for the quorum (20 September 2026)

From the estate harness's second live run against Harness Holdings (report `aer360-harness-2026-09-20-103245.md`, station S6), the estate's audit trail and the compiler's own roster. S6 created two payees, promoted each and had Ada approve once, then expected `whitelisted`; both stayed `pending_promotion`, and the audit row said why — `requiredSignatures 2, signaturesCollected 1`. The charter Harness Holdings confirmed answered C12 = 2 and C12A = No, so the `whitelist_mutation` roster's threshold is two and its members are the census of A8 (`governanceSignersFor`, `onboardingcompiler.ts`): Harriet Founder, Ada Approver, Ben Signatory, Cora Clerk. Four of two is a roster that can meet its quorum; the harness had pressed once and called the estate's honest `pending_promotion` a failure. Read from aeredium/AERAccounts at commit 7d809e1 (Spec 89), `routes/payees.ts`, `services/payees.ts` and `services/onboardingcompiler.ts`.

- `aer360_harness.py`, S6: presses until the quorum is met. After Ada's approval, while the answer's `whitelistStatus` is `pending_promotion` and `approvals.remaining` is above zero, the next roster member approves — the first name in `may_still_approve` for whom the harness holds a signed-in session (excluding the founder, who shares the one credential; a distinct author's press is what moves the count), else Ben Signatory, else the founder. Each press is a step with the presser's name; the loop stops at `whitelisted`, at a refusal, or after `approvals.required` presses, and never presses more times than `WHITELIST_ROSTER` has names. Where the estate answers without `approvals` (before Spec 89), one further press is made as Ben on the charter's quorum of two, with a note. The station's line names each payee's presses in order: *Northwind Supplies: created; promoted; approved by Ada Approver (1 of 2), by Ben Signatory (2 of 2): whitelisted*, and the register's status is read from the row whose `id` is this run's `payee_id`, never by address (every run creates its payees afresh, and earlier runs leave rows with the same addresses still pending).
- `aer360_harness.py`, S6: the first approval's answer is audited (findings of S10's kind, raised in S6, carrying the answer verbatim): *a pending approval that did not say why (Spec 89)* where it lacks `approvals`, `may_still_approve` or `sentence`; *a quorum that exceeds its roster* where `approvals.required` exceeds the names in `may_still_approve` plus `approvals.collected`; and a sentence that does not begin `"<collected> of <required> approvals recorded for this address."`.
- `aer360_answers.py`: `WHITELIST_ROSTER` (the census people, from `CENSUS_ORDER`) and `WHITELIST_QUORUM` (the figure answered at C12), with the compiler's line named, so the harness's expectation is the charter's; a test proves the two agree with the answers given at A8 and C12.
- `aer360_harness.py`, `--dry` and the frozen fixture: S6 carries the second press, *(as the next roster member the first answer names, expected Ben Signatory) → expect whitelistStatus whitelisted*; `tests/fixtures/aer360-dry-calls.txt` is updated for these lines and nothing else (119 → 121 lines).
- Tests: the double's approve answers as Spec 89's estate does — the first press `pending_promotion` with `approvals {required 2, collected 1, remaining 1}`, `may_still_approve` and the sentence, composed word for word; the press by a different roster member `whitelisted`; a press by a key on no seat relayed as `SIGNATURE_NOT_COUNTED` with the platform's words. Knobs: `pending_approval_says_why=False` (the estate before Spec 89), `whitelist_roster` (a roster below its quorum), `platform_never_activates` (a platform that counts every signature and never activates). Each of the spec's tests was red on main.
- A DISAGREEMENT CARRIED TO BEAR, NOT SILENTLY RESOLVED: SPEC.md §6 says "a second press by the same person is refused as the platform refuses a repeated signature", but the shipped Spec 89 code counts the signature once and answers 200 with the same pending body (`routes/payees.test.ts`, "the same person pressing again is told the same thing"); `APPROVER_ALREADY_SIGNED` is the payments road's refusal, never the whitelist road's. §6's own governing clause is "answers as the estate does after Spec 89", so the double answers 200 idempotently and the test asserts that.
- Not touched: corridor_harness.py, series.py, tables.py, aer360_passkey.py, aer360_tables.py and the corridor's tests are not touched; no other station, the answers given at A8, C11 or C12, the passkey code or the tables are changed.

## Spec T8 — The harness expects what the law says (20 September 2026)

From the first live run (report `aer360-harness-2026-09-20.md`) and Bear's rulings of the same morning: three lines of that run said "fail" where the estate was right or where nothing was wrong. The estate's code was read again at aeredium/AERAccounts main after PR #108 (commit dff4c9a, whose `services/onboarding.ts` is e651616's byte for byte) and at Spec 88's branch (commit 35cdc6f, not yet merged).

- `aer360_harness.py`, S11: the venue probe expects acceptance. "Unless it is stipulated explicitly in the questionnaire, it must be accepted" (Bear, 20 September 2026): the questionnaire decides policy, and a contract is an address. HTTP 201 is reported as *accepted, as the law says*, and a refusal is the finding, with the ruling quoted beside the estate's sentence; the probe's docstring carries the ruling and the day the expectation flips.
- `aer360_harness.py`, S10: the read-back is compared by content. `spoken_for` renders in the estate's own forms — `services/onboarding.ts`, the `spoken` switch, read from the code: a list entry's values in the order the estate stores them, a currency as its code, money as the dollars-and-cents sentence, a roster as the names joined by ", " — and returns None for a kind it cannot render, which S10 records as *not compared: no rendering for kind <k>*, a note and never a finding. A list is compared entry by entry, so a finding names the entry.
- `aer360_answers.py`: `as_the_estate_stores`, the shape an answer takes once written — `answers.value` is a `jsonb` column (`db/onboardingschema.ts`), and jsonb keeps an object's keys shortest first, then in byte order — so the census sent as name, email, role is spoken as `name — role — email`, which is what the estate said and what the harness's own rendering, in the order it had sent, called a disagreement.
- `aer360_harness.py`, S12: a slow call is reported and never judged. Every call over two seconds is still listed, the station passes with its numbers, and beside the invitation timings the report says *the invitation road sends the email before it answers, which is where its four seconds go* (`routes/invites.ts` awaits the dispatch before answering 201).
- `aer360_harness.py`, the report: when a previous report stands in the working folder, the closing table gains a *last run* column — the station's previous outcome and each finding's fate, closed, still open or new, matched by station and probe with the ids a run mints set aside — and the findings are listed by fate below the count. `read_report`, `find_last_report`, `finding_key` and `compare_findings` read a report this harness wrote, in Spec T7's shape or this one's. The Python confirms what was fixed.
- `aer360_harness.py`, `--dry`: unchanged in its calls; the venue line's expectation is the law's.
- Tests: the double stores an answer as jsonb does and speaks the currency as main does (JSON, the estate's finding of 20 September) with Spec 88's code behind `currency_spoken_as_code`; `refuses_venue_contract` stands in for the day the questionnaire stipulates against venue contracts, under the estate's own `ADDRESS_PROPOSAL_REFUSED`; `invite_seconds` and a shared clock make an invitation take four seconds where the live road spends them. Each of the spec's tests was red on main: the venue accepted reads as pass and a refusing double as a finding with the ruling quoted; the estate's own census line against the book's JSON entries is no finding and one moved name names its entry; a run with four-second invitations passes S12 with the sentence in the report; two reports in a folder give the closing table its last-run column with closed, still open and new; and the dry run's calls match `tests/fixtures/aer360-dry-calls.txt`, frozen from main after PR #5.
- README: the estate harness section says what the law says and what the harness now expects.
- Not touched: corridor_harness.py, series.py, tables.py, aer360_passkey.py, aer360_tables.py and the corridor's tests are not touched.

## Spec T7 — The estate harness (20 September 2026)

A Python script that walks AER 360 end to end as a founder, from Bear's ruling of 19 September 2026, read from aeredium/AERAccounts main after PR #106.

- New: `aer360_harness.py` — the runner: the invitation and sign-in ceremonies, the interviews answered from the book, the people, the wallet account, the payees, the payments, the journey; the three hats (S10 the auditor, S11 the attacker, S12 the optimizer); the dry run; the report.
- New: `aer360_passkey.py` — the software passkey: a P-256 key on the Mac's own `/usr/bin/openssl`, an attestation object in the `none` format, assertions over `authenticatorData ‖ SHA-256(clientDataJSON)`; CBOR, COSE and DER in pure Python; a pure-Python P-256 verifier and a re-statement of @simplewebauthn's checks, proven against RFC 8949 and RFC 6979.
- New: `aer360_answers.py` — the answer book: every question of the interviews with its kind and answer, the estate, the people, the money and the payments the spec decided; the live catalog's option strings byte for byte.
- New: `aer360_tables.py` — every address the estate harness may send, derived from a fixed seed and checksummed; the venue address S11 sends is read from the corridor's `tables.py` at run time.
- New: `tests/fixtures/aer360-questioncatalog.v11.ts` — the frozen version-11 catalog, copied verbatim, that the book is held against.
- New: `tests/test_aer360_*.py` — the passkey vectors, the book against the fixture, the tables, the dry run, the estate double, the founder's road against it, the three hats.
- README: the estate harness section. `.gitignore`: the report `aer360-harness-*.md` is never committed by accident.
- Not touched: corridor_harness.py, series.py, tables.py and the corridor's tests are not touched.
